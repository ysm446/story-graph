# Changelog

## 未リリース

- 2026-08-01 17:57 **章ビューではじまり / 結末ノードや分岐・島をドラッグで動かせるようにした**(ユーザー報告)。章ビューでは章カード以外のノードを一律ドラッグ不可にしていたため、マーカーや島が動かせなかった。正史の並び順で位置が決まる未分類シーンだけを固定し、それ以外(はじまり / 結末・分岐・島)は動かして位置を保存できるようにした

- 2026-08-01 16:21 **章の並べ替えを追加**した(ユーザー要望)。時系列が未定のエピソードを章として作り置きし、前後を入れ替えて試行錯誤できる。
  - 章ビューで**章カードをドラッグ**(落とした位置で「どの章の後ろか」が決まる)、または**右クリック「← 前へ移動 / → 後ろへ移動」**
  - つなぎ替えは 1 操作でアトミックに行われ、**章ラベルとシーンから生える分岐は保たれ、結末は末尾に追従**する
  - 並べ替えで前提(上流の文脈)が変わる位置から後ろの章のまとめには**「要更新」バッジ**が立つ。下流の状態は自動で再計算され、清書は stale になる(物語上の辻褄 — 前の章を前提にした表現など — は要更新バッジと相談チャットの矛盾チェックで確認する運用)
  - 章カードの右クリックに「章の中を開く / 名前を変更 / 章を解除」も追加。pytest 2 件追加(150 件)。ボタンの文言は当初「解散」だったが**「解除」に変更**(ユーザー指摘)

- 2026-08-01 13:49 **はじまり / 結末ノード方式を導入**した(ユーザー発案。設計: [docs/design/endings.md](design/endings.md))。「正史を選ぶ」から「**結末を決めると、そこから根へさかのぼった道が正史になる**」へ。
  - グラフに 📖 **はじまり**(根に 1 つ・削除不可)と 🏁 **結末**(1 つ以上・最後の 1 つは削除不可)のマーカーノードが加わる。既存ライブラリは開いたときに現在の正史の根・末尾へ自動でつながる(無停止移行)
  - **複数エンディング**: シーンの右クリック「🏁 ここに結末を作る」で結末候補を増やせる(名前も付けられる)。結末の右クリック / インスペクタの「この結末にする」で正史が切り替わる。switch 的な分岐は「複数の子を持つシーン + 各ルートの先の結末」でそのまま作れる
  - **冒頭の分岐が可能に**: はじまりノードが共通の根になるので、最初のシーンの代替案(別のオープニング)も分岐として並べられる
  - シーンの削除・切り離し・つなぎ替えでは、結末が既存のグラフ操作で自然に追従する。アクティブな結末ごと切り離した場合は、はじまり側に結末が自動で作り直される
  - `edges.is_canon` / `nodes.status` は導出キャッシュに格下げ。既知課題「**正史の根が created_at 最古のルートで決まる**」はこれで解消。最初のシーンの削除は「次のシーンがはじまりに直結」に、切り離しは「最初のシーンも島にできる」に挙動が変わった
  - pytest 5 件追加 + 挙動変更 6 件更新(148 件)

- 2026-08-01 11:50 **章カードをダブルクリックしても中に入れない不具合を修正**(ユーザー報告)。React Flow 標準の「ダブルクリックでズーム」と競合してズームだけが動いていた。カード自身がダブルクリックを直接受けて章内ビューを開くようにし、競合するダブルクリックズームは無効化した

- 2026-08-01 11:41 **章を作ると範囲のシーンが消えたように見える不具合を修正**(ユーザー報告)。章カードを毎レンダー導出で作り直していたため、React Flow v12 の実測サイズ(measured)が保持されず、**実測が確定するまでノードを描画しない仕様によって章カードが不可視のまま**になっていた(まとめた範囲はカードに置き換わるので「消えた」ように見える)。章カードをシーンノードと同じく state で保持し、実測・選択状態を保つようにした。あわせて、フラット表示中に章を作った場合も**作成後は章ビューへ切り替える**ようにした

- 2026-08-01 11:35 **「章にまとめる」「章の名前を変更」が反応しない不具合を修正**(ユーザー報告)。名前の入力に `window.prompt` を使っていたが、**Electron は prompt をサポートしておらず呼んだ瞬間に例外**になっていた(入力が出ずに黙って失敗)。自前の入力モーダル(Enter 確定 / Escape 取消)に置き換えた

- 2026-08-01 10:17 **章じまいのまとめ(digest)を追加**した(章グループ設計のフェーズ C 完了。設計: [docs/design/chapters.md](design/chapters.md))。
  - **章ノードを選択すると右のインスペクタが章パネル**になる: 章の情報(名前変更 / シーン一覧 / 解除)と「章のまとめ」(状態バッジ: 未作成 / 最新 / 要更新)。「▶ まとめを作る」で LLM がキャラごとの**要約記憶(memory_compress)**を生成し、その場で文面・重要度を手直しして保存できる
  - **次の章からは、この章の生の記憶の代わりにまとめが引き継がれる**(章境界で fold に合流)。章の中は生のまま。生の記憶は memories に残り、清書時の検索などで引き続き参照される
  - **stale 遮断**: まとめ済みの章の中で記憶の文面などを直しても、章末尾の状態が変わらなければ**清書の stale は章内に閉じる**(まとめは自動では変わらず「要更新」バッジのみ)。まとめを更新して**内容が実際に変わったときだけ**次章側へ波及する
  - 前提として**イベント ID が置換のたびに再発行される既知課題を修正**(イベント編集・再抽出・整合取りで ID を引き継ぐ。`memory_compress.replaces` や関係の reasons の参照が壊れなくなった)
  - DB: `groups.digest_events / digest_stale` を使用(フェーズ B で作成済み)。API: `GET/PUT/DELETE /groups/{id}/digest`、`POST /groups/{id}/digest/generate`。pytest 5 件追加(143 件)

- 2026-08-01 07:15 **章グループを追加**した(章グループ設計のフェーズ B。設計: [docs/design/chapters.md](design/chapters.md))。シーンをエピソード / 章の単位でまとめて扱える。
  - **章にまとめる**: 構造モードで範囲選択 → 右クリック「📖 章にまとめる」。章は正史パス上の連続した並びだけ(分岐・島は不可)。「章から外す」(端のシーンのみ)、章の名前変更・解除(シーンは残る)も可能
  - **構造モードの章ビュー ⇄ 章内ビュー**: 章があるときはドリルダウン型の階層表示になる。章ビューは章カード(第N章 / タイトル / シーン数)が章同士でつながる俯瞰で、**ダブルクリックで章の中へ**。章内ビューはその章のシーン + そこから生える分岐だけを表示し、パンくずで戻る。従来の全シーン表示にも切替可(ツールバーのボタン)。データモデルは変えていない(章は正史区間へのラベルで、章ビューは導出表示)
  - **鑑賞モードの章階層表示**: 縦読み・挿絵分割は章の変わり目に章見出し、ページモードは**章の扉ページ**を差し込む。コントロールバーに**目次**(章の先頭へ移動)。Markdown エクスポートは `# 章 / ## シーン` の階層になる
  - 正史切替・切り離しなどで章の前提(正史上の連続区間)が崩れたときは、章の割り当てを自動で外す。章の真ん中への挿入は章を引き継ぐ
  - DB: `groups` テーブルと `nodes.group_id` を追加(冪等 ALTER。既存 DB はそのまま動く)。API: `GET/POST/PATCH/DELETE /groups`。pytest 5 件追加(138 件)

- 2026-08-01 06:29 **シーン編集の stale 波及を細かくした**(章グループ設計のフェーズ A。設計: [docs/design/chapters.md](design/chapters.md))。従来はどのフィールドを編集しても下流全シーンの状態キャッシュと清書が stale になっていた。
  - **ビート本文・題・場所・時期などの編集**(プロンプト文面のみ)→ **自シーンの清書だけ stale**。下流の状態・清書は保たれる(誤字修正で全編 stale がなくなる)
  - **cast の編集** → 再 fold して**状態が実際に変わったときだけ**下流へ波及(early cutoff)。並べ替えや登場済みキャラの追加では波及しない
  - イベントの編集・再抽出、挿入・削除・つなぎ替え・正史切替は従来どおり下流まで波及(状態が変わる操作のため)
  - pytest 3 件追加 + 既存 1 件を新仕様に更新(133 件)

- 2026-07-31 21:32 インスペクタの**清書タブを改善**した(ユーザー要望)。
  - **本文の見た目をシーンタブと揃えた**(UI フォント 13px・行間 1.6 相当)。従来は鑑賞モードと同じ本文用フォント + 可変サイズ + 行間 1.9 で、インスペクタ内で浮いていた。鑑賞モードの表示は従来どおり
  - **生成統計を表示**するようにした(チャットの返事と同じ並び: `⚡ tok/sec · 💬 tokens · 🕐 s · Finish reason`)。清書が使う `llm.chat_stream` は統計を捨てていたので、受け口(`stats_out`)を追加して `renders.meta` に保存する
  - **生成に送った内容(システムプロンプトなど)を 📄 アイコンから確認できる**ようにした(チャットと同じモーダル)。送信 messages の控えを `renders.prompt_messages` に丸ごと保存する。どちらも列追加以前の清書には無いため表示されない(清書し直すと付く)
  - チャットの `StatsLine` / `SystemPromptModal` / ホバーアイコンは `GenMeta.tsx` に切り出して共用。pytest 2 件追加(130 件)

- 2026-07-31 21:00 チャットの**返事から、その生成時に LLM へ実際に送った内容(システムプロンプト + それまでの履歴 + ツール結果)を見られる**ようにした(ユーザー要望)。返事の下のホバーアイコン(📄)で、設定画面「プロンプトログ」と同じ role ごとの形式のモーダルを開く。相談チャット・キャラ会話の両方に対応。プロンプトログは直近 30 件のメモリ上のログなので古い返事は追えなかったが、こちらは送信内容の控えを assistant メッセージの `prompt_messages` に丸ごと保存するので、`ts`(発言時刻)と同じように**あとからいつでも確認できる**(この変更より前の履歴には控えが無いためアイコンは出ない)。履歴が長くなると返事ごとに全文が積まれてデータは嵩むが、正確な記録を優先する方針(ユーザー決定)。控えは `meta` / `ts` と同様に LLM への送信からは除外する(回帰テスト追加。pytest 128 件)

- 2026-07-30 09:50 設定画面に**「スタイルプリセット(清書の文体指示)」セクションを追加**した(ユーザー要望)。登録済みプリセットの一覧(組み込み / カスタムのバッジ、人称)を出し、そこから**編集・新規作成・削除**ができる。編集モーダルは鑑賞モードや清書タブの ✎ と同じもの([RenderStyle.tsx](../src/renderer/src/RenderStyle.tsx) の `PresetEditorModal` を共有)。従来は「使うプリセットを選ぶ場所」からしか編集に入れず、文体を腰を据えて書くのに向かなかった。**鑑賞モード・清書タブの ✎ / + はそのまま**(清書を見ながら直す流れは残す)

- 2026-07-30 09:44 **プロンプトの書き出し / 読み込みを設定画面にまとめた**(ユーザー要望)。従来は鑑賞モードのバーに「⬆ 書き出し / ⬇ 読み込み」があり、**スタイルプリセットだけ**を `style-presets.json` に出す機能だった。設定画面に「プロンプト入出力」セクションを新設し、**シーン生成プロンプト・校正プロンプト・カスタムのスタイルプリセットを 1 つの JSON**(`story-graph-prompts.json`)にまとめて扱うようにした。プロンプトはライブラリごとの設定なので、別のストーリーに持ち込むのが主な用途。読み込み時はスタイルプリセットを**新規として追加**(既存・組み込みは変わらない)、シーン生成・校正のプロンプトは**置き換え**で、何が起きるかを確認ダイアログに出す。**以前の `style-presets.json` もそのまま読める**(配列 / `{presets}` 形式を受け付ける)。鑑賞モードのバーからは 2 ボタンを削除してすっきりさせた(`RenderStyleControls` の `onStatus` prop も不要になったので削除)

- 2026-07-30 09:37 鑑賞モードの**文字送り(ページモードで文字が流れる速さ)を選べる**ようにした(ユーザー要望)。`一気に表示 / 速い(500字/秒)/ 標準(250)/ ゆっくり(110)/ とてもゆっくり(45)`。従来は 250 字/秒の固定だった。選択はライブラリごとの settings(`reader_typing_speed`)に保存する。コントロールバーの選択欄は**ページモードのときだけ**出す(縦読み・挿絵分割には文字送りの演出がないため)。あわせて**クリックで全文表示しても途中まで戻ってしまう不具合を修正**した(タイマーを止めていなかったため、次の tick で表示が巻き戻っていた。速さを遅くすると顕著になる)

- 2026-07-30 09:30 設定画面を**左にセクション一覧、右にそのセクションの設定**という 2 ペイン構成にした(ユーザー要望)。従来は 9 グループ(推論エンジン / 推論パラメータ / シーン生成プロンプト / 校正 / 構造モード / 相談チャット / 鑑賞モード / バックアップ / プロンプトログ)が縦一列に積み上がっていて、目的の設定まで長くスクロールする必要があった。左ナビの見た目は資料庫のサイドバーと揃えた(選択中はアクセント色の帯)。選んでいたセクションは `localStorage.settingsSection` に覚えるので、開き直しても同じ場所に戻る。「保存しました」の合図はどのセクションでも同じ位置に出るよう左下に移した。**選択中のセクションだけをマウントする**ので、スナップショット一覧とプロンプトログの取得もそのセクションを開いたときだけ走る

- 2026-07-30 00:50 **スナップショット(バックアップ)を追加**した(ユーザー要望。設計: [docs/design/snapshots.md](design/snapshots.md))。undo の代わりに「時点に戻す」を提供する。
  - **自動保存**: 危険な操作の前に自動でライブラリ全体(`story-graph.db`)を `snapshots/` に複製する(`VACUUM INTO`)。契機はシーン削除(30秒スロットル)/ 正史切替(毎回)/ イベント編集(10分)/ シーン編集(10分)/ つなぎ替え(60秒)/ イベント作り直し(60秒)。一括削除のような API 連打はスロットルにより最初の 1 回だけ保存される。自動分は直近 30 件まで
  - **手動保存**: 設定画面の「バックアップ」セクションに「今の状態を保存」(任意の名前付き)。手動分は無期限
  - **復元**: 一覧から選んで確認 → その時点の DB に差し替えて画面を読み直す。復元の直前の状態も「復元の前」として自動保存されるので、復元自体も取り消せる
  - `gc_assets` はスナップショットが参照する画像も回収対象から守る(復元後に挿絵が消えないように)
  - API: `GET/POST /snapshots`、`POST /snapshots/{id}/restore`、`DELETE /snapshots/{id}`。pytest 7 件追加(127 件)。12B 相当の実機 API でも保存→復元→巻き戻しの往復を確認済み

- 2026-07-30 00:25 相談チャットの**左サイドバー(会話一覧)の既定幅を 208px → 264px** に広げた(ユーザー要望)。会話名と 2 行目(キャラ名 / アンカーのシーン名)が切れにくくなる。ドラッグで変えた幅は従来どおり `localStorage.chatSidebarWidth` に残るので、**既に幅を調整したことがある場合は変わらない**(初期値のみの変更)。最小 140px / 最大 420px も従来どおり

- 2026-07-29 23:25 `GET /graph` の **N+1 クエリを解消**した(処理の軽量化)。従来はノードごとに `get_node`(ノード+イベントで SELECT×2)を回していたため、100 シーンなら約 200 クエリだった。全ノード 1 クエリ+全イベント 1 クエリ+全エッジ 1 クエリの一括組み立てに変更(シーン数によらず 3 クエリ)。構造モードは操作のたびに reload でここを引くので、シーンが増えたときの機敏さに効く。get_node と形が一致し続けることの回帰テストを追加(pytest 120 件)

- 2026-07-29 23:24 イベント保存前の「サーバーの最新イベントを取り直す」処理を、全グラフ取得(`GET /graph`)から**単一ノード取得(`GET /nodes/{id}`)に変更**した(処理の軽量化)。手動イベントの追加・編集・削除・退場設定のたびに全ノード+全イベントを引いていたのが 1 ノード分で済む。api.ts に `getNode` を追加

- 2026-07-29 23:23 llama-server に **`--cache-reuse 256` を追加**し、前リクエストと共通するプロンプト接頭辞の KV キャッシュを再利用するようにした(処理の軽量化。ユーザー要望)。清書・生成・チャットのプロンプトは「システムプロンプト+キャラプロフィール+状態」の長い共通部分を持つため、連続清書やチャットの継続ターンでプロンプト処理(prefill)が大きく削れる。31B ではプロンプト処理が数秒〜十数秒かかるので体感に効く。**次回のモデルロードから有効**(既に起動中の llama-server には効かないので、モデルを一度停止→ロードし直す)

- 2026-07-29 23:08 **アプリの二重起動を防止**した(`app.requestSingleInstanceLock`)。2 つ目を起動すると即終了して既存ウィンドウを前面に出す。二重に起動すると userData の Chromium キャッシュを取り合い、起動ログに GPU キャッシュの「アクセスが拒否されました (0x5)」が並んでいた(ユーザー報告。実害は GPU シェーダーキャッシュが効かないだけだが紛らわしい)

- 2026-07-29 23:03 相談チャットの**会話一覧にフォーカスがあるとき、Delete キーで選択中の会話を削除**できるようにした(ユーザー要望)。⋯ メニューの「削除」と同じ処理(確認ダイアログあり)。このとき構造モード側の Delete(選択ノードの削除)には届かないよう伝播を止める。名前の変更中(入力欄)は文字の削除に任せる

- 2026-07-29 22:56 **world facts を「誰もが知っている公のこと」専用に確定**した(「誰が何を知っているか」問題の方針 (a) を実装。ユーザー決定)。従来は `真犯人=執事` のような秘密も world facts に入り得て、キャラチャットで本人が知らないはずのことを語り出す穴があった。① **LLM の抽出スキーマから `scope="world"` を削除**(シーン生成・イベント抽出とも。world facts は作者が手動イベントで意図的に置くものに。EVENT_RULES も「秘密は知っているキャラの char fact に書く」形に更新)、② キャラチャットは world facts を「(世間で知られていること)」として本人に渡す(公のことなので正当)、③ **POV 清書でも world facts を出す**ようにした(従来は隠していたが、公のことなら視点キャラも知っている)。既存データの world facts はそのまま有効(fold・手動イベント UI は変更なし)。スキーマの回帰テストを追加(pytest 119 件)

- 2026-07-29 22:39 **プロジェクト全体のコードレビューを行い、見つかった不具合をまとめて修正**した(バックエンド 22 件 + フロントエンド 31 件)。ユーザーから見える主な修正:
  - **分岐の根を削除すると正史が壊れることがある**のを修正(付け替えエッジが canon のまま親に移り、正史エッジが二重になっていた。以後は draft に落として付け替える)
  - **場所の説明・雰囲気を編集しても既存の清書が「最新」のままだった**のを修正(直接参照するシーンと、空欄で引き継いでいる子孫シーンの清書を stale にする。削除時の対象も引き継ぎ子孫まで広げた)
  - **相談チャットが途中で失敗・切断すると、その往復が丸ごと消えていた**のを修正(途中までの履歴を必ず保存)
  - **保存前のイベント編集を連続で行うと片方が消えることがある**のを修正(全件置換の土台をサーバーの最新イベントに)
  - **LLM タスクをキューに積んだまま他モードへ移って戻ると、完了しても画面に反映されなかった**のを修正(グラフ変更通知と busy 表示をモジュールレベルへ移動)
  - **ドラッグ直後にノードが旧位置へ戻って見える**スナップバックを修正(楽観更新)
  - **エッジを選択したままシーンを選んで Delete すると、シーン削除のつもりが切り離しになる**のを修正
  - **llama-server の二重起動**(生成とチャットが同時に自動ロードしたとき)と、**停止失敗時にプロセスが残る**問題を修正(排他ロック + 停止確認)。設定画面のモデル停止・切替中に他の操作が固まるのも解消
  - **起動直後に記憶検索を使うと、埋め込みモデルのロード完了まで全 API が固まる**ことがあったのを修正(ロード中は FTS のみで続行)
  - **記憶が増えると検索候補が上位 50 件から押し出されて想起されなくなる**問題を修正
  - **設定画面を開いたまま ModelBar でモデルを変えると、設定保存時に巻き戻る**のを修正(変更キーだけ保存)
  - llama.cpp 自動インストールの失敗時に**壊れたサーバーが残って次回の起動が原因不明で失敗する**のを修正(失敗・キャンセル時に展開先を削除)。ダウンロードの無通信ストールにタイムアウトを追加、進捗 SSE を 1% 刻みに間引き
  - ほか: SSE ストリーム途中のサーバーエラーが握り潰されて途切れた清書が成功扱いになる問題、イベント保存が途中で失敗したときに半端な書き込みが後続の操作で確定する問題(エラー応答時のロールバック)、memory_compress の要約が検索から消える問題、各種エラーの沈黙(追加・接続・整列・読み込み失敗などをステータスに表示)、リサイズ・ストリームの後始末漏れ、チャットの自動スクロールが過去ログ閲覧中に飛ぶ問題など。pytest 118 件(回帰テスト 5 件追加)、typecheck 通過

- 2026-07-29 22:05 **生成が引き起こしたモデルの自動ロードを、モデル選択バーに映す**ようにした(ユーザー要望)。モデル未ロードのまま清書やシーン生成を始めると `llama_manager.ensure_running` が llama-server を spawn するが、バーは「モデルをロードしてください」のまま数十秒(31B なら特に長い)沈黙し、何が起きているか分からなかった。`GET /llm/status` に **`loading`**(= spawn 済みだが健全応答がまだ → 読み込み中)を足し、バーはそれを見て**自分でロードしたときと同じ見た目**(スピナー + 時計まわりのリング + 「読み込み中… モデル名」)にする。モデル名は `status.model_path` から取るので、設定を外から変えられていてもバーが嘘をつかない。読み込みが終わったら実際にロードされたモデルに選択表示を合わせる。ポーリングは `setInterval` から自己再帰の `setTimeout` に変え、**読み込み中だけ 4 秒 → 1.5 秒**に詰める(終わった瞬間の反映を速くする)。読み込み中はモーダルを開けない(自分でロードしたときと同じ)。(ユーザー要望)。全体一律だと「日常回は短く、クライマックスは長く」という書き分けができなかった。`nodes.target_chars`(冪等 ALTER。NULL / 0 = 共通の設定に従う)を足し、構造モードの清書タブの分量プルダウンを**そのシーンの指定**に変えた(先頭の選択肢が `分量: 共通に従う(標準(~1200字))` のように共通の現在値を出すので、何が効くか読める)。**共通の設定は鑑賞モードのバーで変える**(全体の既定値)。解決はサーバー側(`rendering.effective_target_chars`)で行うので、鑑賞モードの「全編を清書」も右クリックの一括清書も**シーンごとの指定を尊重して 1 本のストリームで書き分ける**。保存は専用の `POST /nodes/{node_id}/target_chars`(`set_node_image` と同じ形)で、**state を汚さず既存の清書も stale にしない**(分量は fold に無関係で、作り直すかは作者が決めるため)。清書タブの字数表示は `1180 字 / 目安 1800` となり、ツールチップで「このシーンの指定」か「共通の設定」かが分かる。プルダウン本体は `LengthSelect` として共通化した(共通用と個別用の違いは 0 のときの文言だけ)。

- 2026-07-29 21:30 清書の分量を**シーンごとに指定できる**ようにした(ユーザー要望)。全体一律だと「日常回は短く、クライマックスは長く」という書き分けができなかった。`nodes.target_chars`(冪等 ALTER。NULL / 0 = 共通の設定に従う)を足し、構造モードの清書タブの分量プルダウンを**そのシーンの指定**に変えた(先頭の選択肢が `分量: 共通に従う(標準(~1200字))` のように共通の現在値を出すので、何が効くか読める)。**共通の設定は鑑賞モードのバーで変える**(全体の既定値)。解決はサーバー側(`rendering.effective_target_chars`)で行うので、鑑賞モードの「全編を清書」も右クリックの一括清書も**シーンごとの指定を尊重して 1 本のストリームで書き分ける**。保存は専用の `POST /nodes/{node_id}/target_chars`(`set_node_image` と同じ形)で、**state を汚さず既存の清書も stale にしない**(分量は fold に無関係で、作り直すかは作者が決めるため)。清書タブの字数表示は `1180 字 / 目安 1800` となり、ツールチップで「このシーンの指定」か「共通の設定」かが分かる。プルダウン本体は `LengthSelect` として共通化した(共通用と個別用の違いは 0 のときの文言だけ)。

- 2026-07-29 20:36 清書に**分量(目安の字数)の指定**を追加した(ユーザー要望)。スタイルプリセット / POV と同じ列にプルダウンを置き、`おまかせ / 短め(~600字) / 標準(~1200字) / 長め(~2000字) / 字数を指定…`(100〜5000字の数値入力)から選ぶ。鑑賞モードと構造モードの清書タブで**共通**(settings の `reader_target_chars`)で、右クリックの一括清書にも効く。プロンプトには人称の次に `分量: 日本語で 900〜1500 字程度(目安。話の切れ目を優先し、無理に埋めたり削ったりしない)` のように**目安の ±25% を幅**として渡す(数値ぴったりを狙わせると不自然に伸縮するため)。あわせて**生成上限を字数から算出**する(`max_tokens = 字数 × 2.5`、1024〜12288 に制限。従来は 4096 固定だったので、2000 字以上の指定では途中で切れていた)。おまかせのときは従来どおり分量指示なし・4096。清書タブの字数表示は `1180 字 / 目安 1200` の形になり、指示がどれくらい効いたか分かる。**ローカル LLM は字数を厳密には守らない**ので、あくまで目安。

- 2026-07-29 19:58 構造モードのインスペクタに**「清書」タブを追加**した(ユーザー要望)。選択中のシーンの散文をその場で見て、その場で書き直せるプレビュー。鑑賞モードと**同じ `renders` を共有**しているので、ここで書いたものはそのまま鑑賞モードに出る(逆も同じ)。**スタイルプリセットと POV のプルダウンも共通**にした([RenderStyle.tsx](../src/renderer/src/RenderStyle.tsx) に `useRenderStyle` / `RenderStyleControls` として切り出し、鑑賞モードのコントロールバーもこれを使う)。選択はライブラリごとの settings(`reader_preset_id` / `reader_pov_char` / `reader_font` / `reader_font_size`)に保存されるので、どちらの画面から変えても同じ条件になり、右クリックの一括清書もその条件で走る。生成は既存のタスクキューに積む(1 件ずつ逐次、ステータスバーから中止可、対象ノードの枠が光る)。本文はストリーミング表示、フォントと文字サイズも鑑賞モードの設定を共有する。バックエンドは単一シーンの清書を返す `GET /renders/{node_id}` を追加(既存の `GET /renders` は正史パス限定なので、**分岐や島のシーンでも取れる**新しい入口が要る)。正史パス外のシーンでは「直前シーンの文末に接続する処理は効かない」旨をタブ内に出す。

- 2026-07-28 関係図で、**エッジの線が矢じりを突き抜けて見える**のを直した(ユーザー指摘)。SVG の `marker` + `markerEnd` は終点に矢じりを置くだけで線は終点まで引かれたままなので、三角形の下を線が通り抜けていた。三角形は先端に向かって高さが 0 に細るため、線の太さ(`1 + |score| × 3`、最大 4px)が上回る先端付近では線がはみ出してトゲのように見えていた。さらに `fill="context-stroke"` は色だけを引き継ぎ `stroke-opacity` は引き継がないため、半透明の線に対して矢じりだけが不透明になり、重なりが濃い帯として見えていた。marker をやめて**矢じりを自前の `<polygon>` にし、線を矢じりの根元で止める**方式に変更(`trimEnd()` / `arrowHead()` を追加。折れ線の最終セグメントが短ければ手前のセグメントも順に消費するので、直角ルーティングでも破綻しない)。矢じりの寸法は旧 marker と同じ比率(長さ・根元幅とも線の太さの 4.5 倍)なので見た目の大きさは変わらない。矢じりの不透明度は線と揃えた。資料庫の関係図タブとインスペクタのクイックビューの両方に効く(共通コンポーネント)。

- 2026-07-28 パネル間の**分割線がぼやけて見える**のを直した(ユーザー指摘)。従来は幅 4px の当たり判定ハンドルがレイアウトを占有し、その中に 1px のラインを `left-1/2` + `-translate-x-1/2` で浮かせていたため、ラインの左端が 1.5px という半端な座標になり、1px の線が 2 ピクセルに薄く塗り分けられていた(transform をかけた要素はブラウザのピクセルスナップからも外れる)。**ラインをレイアウト実体の 1px にして、当たり判定だけを絶対配置で左右(上下)に 3px はみ出させる**構成に変更。パネル同士が 1px の境界で接するようになり、ドラッグ幅は 4px → 7px に広がった。ホバーの色変えは当たり判定を DOM 上の子孫にすることでライン側の `hover:` がそのまま効く(`group-hover` は不要になった)。対象は相談チャットの会話一覧、資料庫のサイドバー、構造モードのインスペクタとチャット領域上端の 4 か所。

- 2026-07-28 ノードエリアで、**構造を変えてもノードが勝手に動かない**ようにした(ユーザー要望)。従来は `pos_x/pos_y` が NULL のノード(= 一度もドラッグしていないノード)を毎回 DAG レイアウトで置き直していたため、エッジを切る・繋ぐ、正史を切り替える、シーンを追加・削除するたびに深さとレーンが計算し直され、画面全体が並び替わっていた。**一度画面に置いたノードはその位置を保つ**(新しく現れたノードだけレイアウト計算する)ようにし、さらに高さが測れて位置が落ち着いた時点で、その座標を保存座標として焼き付ける(新設の `POST /layout/positions` に 1 リクエストでまとめて送る)。次に開いたときも同じ場所に出る。自動レイアウトに戻したいときはツールバーの ⟲。

- 2026-07-28 ツールバーの **⟲ で「選択中のシーンだけを整列」**できるようにした(ユーザー要望)。2 つ以上のノードを選択中に押すと、選択したノードとその間のエッジだけで DAG レイアウトを計算し、**選択範囲の左上を動かさないまま**並べ直す(選択外のノードは触らない)。選択が 1 つ以下のときは従来どおり、手動配置をすべて捨てて全体を自動レイアウトに戻す。

- 2026-07-28 相談チャットの**左サイドバー(会話一覧)の幅を分割線のドラッグで変えられる**ようにした(ユーザー要望)。従来は `w-52`(208px)固定で、長い会話名やシーン名が読めなかった。境界の当たり判定と見た目は構造モードのインスペクタ分割線と同じ(幅 4px / 1px のライン / ホバーでアクセント色)。幅は 140〜420px に収め、会話エリアが 320px を下回らないようドロワー幅からも上限をかける。値は `localStorage.chatSidebarWidth` に覚える(モードを切り替えるとドロワーは破棄されるため)。

- 2026-07-28 相談チャットの会話一覧で、**キャラ会話の目印をキャラ本人のアバターにした**(ユーザー指摘)。従来は 12px の線画の仮面アイコンで、小さすぎて誰との会話か分からなかった。ヘッダーと同じ `CharAvatar`(20px。画像が無ければキャラ色の頭文字)を使い、相談チャットには吹き出しアイコンを同じ幅の枠で置いて見出しの開始位置を揃える。2 行目にキャラ名も出すようにした(従来はアンカーのシーン名のみ)。

- 2026-07-28 ノードエリアの右クリックメニューに**「清書(この先も / 未清書のみ)」「清書し直す(この先も / 上書き)」を追加**した(ユーザー要望)。「イベントを作り直す(この先も)」と同じく、選択シーンとその下流すべてを対象にする。下流の収集はフロントで行い(全子孫)、実行順はサーバーが正史・深さ順に並べ直す。想定する使い方は「イベントを作り直す(この先も)→ 清書(この先も / 未清書のみ)」の 2 手ですべてをレンダー済みにする流れ: イベントの変更は dirty 伝播で下流の renders を stale にするので、「未清書のみ」でも作り直した範囲がちゃんと拾われる。

- 2026-07-28 資料庫の関係図タブを開いたとき、**ノード全体がエリア中央に収まるようフィット表示**するようにした(ユーザー要望)。従来は論理キャンバスの原点からの固定表示で、ノードの配置(インスペクタ版と共有の保存座標)によっては端に寄ったり見切れたりしていた。全ノードの配置が確定した時点で外接矩形+余白からズーム(0.2〜1 に制限。少人数でも拡大しすぎない)と中心を計算する。背景ダブルクリックのリセット先もフィットに変更(インスペクタ版は従来どおり原点リセット)。ホイールズームの下限を 0.4 → 0.2 に緩めてフィット倍率と揃えた。

- 2026-07-28 資料庫の関係図タブを**「その時点で登場済みのキャラ全員」を表示する相関図**にした(ユーザー指摘)。従来は関係イベントに関わったキャラしか描かれず(インスペクタ版から引き継いだスパース表示)、関係が少ない序盤だと数人しか出ないため「全体の相関図」に見えなかった。関係ゼロのキャラも孤立ノードとして置き(「まだ誰とも絡んでいない」ことが見える)、時間シークバーを動かすと登場に合わせて人が増えていく。|score| しきい値は線を減らすだけでキャラは消さない。エゴ絞り込み中は従来どおり当事者のみ。**インスペクタ版は変更なし**(関係があるペアだけのクイックビューのまま)。

- 2026-07-28 資料庫に**「関係図」タブ(フル版のキャラクター関係図)を追加**した(ユーザー要望。progress の「フル版の関係図」Step 1)。インスペクタ内のコンパクト版より大きいキャンバス(880×620)で、時間スクラブ・|score| しきい値・エッジ履歴ドリルダウン・ズーム/パン/ドラッグ配置はそのまま使える。エッジは**経路探索(グリッド A*)による 90° 直角折れ線**: ノードの周囲を通行止めにして上下左右のみで経路を探索し、曲がりに直進優先のコスト、他のエッジが通過済みのセルに通行税を付けることで、ノードを避けつつ並走エッジが別レーンに分かれる(Lucidchart 風)。サイドバーはキャラ一覧になり、クリックでそのキャラのエゴネットワークに絞り込める。ノード座標はコンパクト版と共有(どちらで動かしても同じ配置)。インスペクタ版は従来どおり直線のクイックビューとして残す。

- 2026-07-28 関係図で、**score が 0 に戻った関係が消えないようにした**(ユーザー指摘)。fold 層は score 0 でも関係エントリと reasons(イベント履歴)を保持しているのに、表示側が `score === 0` を一律に描画対象から外していたため、恩と裏切りが相殺して 0 になった関係が「接点が一度もないペア」と同じ見た目になっていた。イベントが発行された関係(reasons が非空)は score 0 でも**中立グレーの破線**で表示する(弱いプラス/マイナスの実線と区別)。クリックでの reasons ドリルダウンはそのまま使えるので「なぜゼロに戻ったか」を追える。しきい値スライダーを 0 より上げたときは他の弱い関係と同様に消える。空表示の文言も「score 非ゼロの関係はありません」→「表示できる関係はありません」に変更。

- 2026-07-28 鑑賞モードの**「シーンに取り込む」(ビート昇格)を削除**した(ユーザー判断)。散文を 4 文字以上選択するたびにモーダルが開く仕組みだったため、気に入った文章をコピーしたいだけの操作と毎回衝突していた。実際に一度も使われておらず、今後も使う見込みがないため、フロントの選択ハンドラ・モーダル・`api.promotePreview`、バックエンドの `/nodes/{id}/promote_preview`・`rendering.promote_preview`(`PROMOTE_PROMPT` / `promote_schema`)とテストをまとめて撤去した。散文で生まれた要素を正史に残したい場合は、構造モードでビートを直接編集する(spec の「散文→正史への唯一の還流経路」という位置づけも廃止)。

- 2026-07-28 鑑賞モードのページモードで、**ページの 1 行目が改行だけのときに詰める**ようにした(ユーザー要望)。ページ分割は「きりの良い位置」として改行の位置で切るが、その改行文字が次のページの先頭に残るため、ページをめくるたびに 1 行ぶん空いていた。ページの先頭の改行(`\n` / `\r`)を読み飛ばしてから収まる分量を測る(**字下げの全角空白は残す**)。詰めた分だけ 1 ページに入る量も増える。改行だけの清書でシーンのページが消えないよう、1 ページも作られなかったシーンには空のページを 1 つ持たせている(挿絵の表示とシーン指定のジャンプに必要)。

- 2026-07-28 校正で置換した後に出る**「↩ 元に戻す」を本文に重ねない**ようにした(ユーザー要望)。テキストエリアの右上に絶対配置していたため、1 行目の右端が読めなくなっていた。テキストエリアの**下に右寄せ**で出す(エラー表示と同じく通常フローに置く)。

- 2026-07-28 相談チャットの**入力欄を、書いた内容に合わせて縦に伸ばす**ようにした(ユーザー要望)。従来は `rows={1}` の固定で、長い相談を書くと 1 行の中でスクロールしていた。シーン本文(`ProofreadTextarea`)や感情の核と同じ「`height: auto` → `scrollHeight`」方式。会話が潰れないよう**ドロワーの高さの 40%**(最低 64px)を上限にし、超えた分はスクロールする。幅の変化(インスペクタのリサイズ)とドロワーの高さの変化(分割ペインのドラッグ)でも計算し直す。

- 2026-07-27 シーンインスペクタの**「⟳ ここから先のイベントを作り直す(LLM)」を削除**した(ユーザー提案)。ノードエリアの右クリック「イベントを作り直す(この先も / LLM)」が同じ操作(そのシーンと下流すべて)なので、入口が二重になっていた。あわせて、このボタンだけが使っていた `runReextractChain` / `countSubtree` と、どこからも読まれなくなっていた `reextracting` state(進捗はタスクキュー経由でステータスバーに出る)を落とした。バックエンドの `/nodes/{id}/reextract_chain` と `api.reextractChainStream` は残してある。

- 2026-07-27 シーンインスペクタの**「自動生成」ボタンのアイコンをフラットにした**(ユーザー要望)。絵文字の ✨ は環境依存の多色表示になり、他のアイコン(⟲ / ▶ / ⑂ など単色の図形)と質感が揃っていなかったため、共有アイコンセットの `sparkle`([icons.tsx](../src/renderer/src/icons.tsx)、単色・`currentColor` の線画)に置き換えた。Title と Emotional core の 2 か所。

- 2026-07-27 モデル**読み込み済みのモデル選択バーの枠を明るく**した(ユーザー要望)。従来の `--accent-border`(不透明度 0.35)は背景に埋もれて、ロード済みかどうかが遠目で分かりにくかった。`--accent-border-bright`(rgba(140, 110, 255, 0.75))を足し、バーの枠だけに使う(ノードや他の枠は従来どおり)。

- 2026-07-27 矢印キーで続けてシーンを移動すると、**インスペクタのタイトル / シーン / Emotional core / Cast / 場所が前のシーンのまま残る**不具合を修正(ユーザー報告)。原因は未保存下書きの一時退避(`beatDraftCache`)の書き込み先の取り違え。シーンが切り替わった直後の 1 レンダーでは `draft` state がまだ前のシーンの値なので、退避用の effect が **前のシーンの内容を移動先のシーンの下書きとして保存**していた。通常は次のレンダーで掃除されるが、掃除が走る前にもう一度移動すると(矢印キーの連打)、通り過ぎたシーンに間違った下書きが残り、次にそのシーンを開いたときに「未保存の下書き」として復元されていた。`draftNodeId` を持たせ、**draft が現在のシーンのものになるまで退避しない**ようにした(`dirty` の判定も同様に抑える)。この 5 項目だけがずれて挿絵やイベントは正しく切り替わるのは、退避の対象がこの 5 項目だったため。

- 2026-07-27 モデル選択バーの**読み込み中の表示を、ノードの LLM 処理中と同じ「時計まわりに光る枠」**にした(ユーザー要望)。従来は `animate-pulse`(全体の明滅)で、ノード側の演出と揃っていなかった。`.node-busy-ring` のはみ出し量・角丸・太さ・グローの強さを CSS 変数(`--sg-ring-inset` / `--sg-ring-corner` / `--sg-ring-width` / `--sg-ring-glow`)に切り出し、ノードカード以外にも付けられるようにした(既定値は従来のノードカード = `rounded-3xl` のままなので見た目は変わらない)。バーは小さいのでリングを 3px → 2px、グローを 14px → 10px に落としている。

- 2026-07-27 F12 で撮ったスクリーンショットの**通知をクリックすると保存先を開く**ようにした(ユーザー要望)。右下の通知にファイル名を出し、クリックでエクスプローラーがそのファイルを選択した状態で開く(`shell.showItemInFolder`)。クリックする余裕を持たせるため表示は 2.5 秒 → **6 秒**に延ばし、すぐ消したいとき用に ✕ を付けた。IPC は `shell:reveal`(実在しないパスは何もせず `false` を返す)。

- 2026-07-27 候補質問を **3 件 → 5 件**に増やした(会話と一緒にスクロールするようになり、固定枠だった頃の高さの制約が無くなったため)。内容から生成された質問の枠も 2 → **3 件**に広げた(生成側の上限が 3 件なので、出たものは全部使う)。5 件を並べても入れ替えが意味を持つよう、**固定の候補を 6 → 10 件**に増やした(相談: 山場 / 弱いところ / 各人の望み / 場所の使い方、キャラ: 知っていること / 言えていないこと / この場所 / 変わったこと)。あわせて、⟳ の送り幅がキャラモードでも `TEMPLATES.length` で折り返していたのを、表示中の配列の長さで折り返すよう直した。
- 2026-07-27 「ほかの候補を見る」(⟳)も候補チップの下へ移した。入力欄の並びから外れて、候補の入れ替えという役割どおりチップの延長に置いた(ラベルも「⟳ ほかの候補」にした)。
- 2026-07-27 チャットの**候補質問チップを会話エリアの中(末尾)に移した**(ユーザー要望)。従来は会話エリアと入力欄の間の固定枠だったため、チップ 3 件ぶんの高さが常に会話から差し引かれていた。会話と一緒にスクロールするようになり、狭いドロワーでも本文に高さを使える。チップが差し替わったときも下端に寄せるようにした(会話の末尾にあるため)。

- 2026-07-27 チャットの吹き出しの上に**発言時刻**(「7/20 12:05」)を表示するようにした(ユーザー要望)。バックエンドが保存時に UTC の ISO 8601 で `ts` を付け、表示側でローカル時刻に直す(ホバーで完全な日時)。`ts` は `meta`(生成統計)と同じく**保存用の付加情報で LLM には渡さない**。時刻を持たない既存の履歴では何も出さない。送信直後・ストリーミング直後の吹き出しにもその場で時刻を出す(再読み込み後はサーバーが付けた値に置き換わる)。

- 2026-07-27 相談チャットの**開閉状態を保持する**ようにした(ユーザー要望)。モードを切り替えると `StructureMode` は破棄されるため、開いたままにしていても構造モードに戻ると閉じていた。高さ(`chatDrawerHeight`)と同じく localStorage に覚える(既定は閉じた状態)。閉じる操作はドロワーの ✕ もトグルボタンも同じ経路(`toggleChat`)を通るので、どちらで閉じても記憶される。

- 2026-07-27 選択シーンを切り替えても**インスペクタに前のシーンの情報が残る**不具合を修正(ユーザー報告。矢印キーでの移動を入れて顕在化した)。原因は非同期取得の後始末不足で、キャラ状態(`api.getState`)・検証結果(`api.validateNode`)・関係図の状態を取り直す際に **古い応答を捨てていなかった**ため、素早く切り替えると後から返ってきた前のシーンの結果が残り続けていた。それぞれ `ignore` フラグで打ち切るようにし、キャラ状態は取得中に前のシーンの値を出したままにしないようクリアしてから取り直す。あわせて、シーンを切り替えたときに**書きかけの入力を持ち越さない**ようにした(キャラタブの記憶・関係・fact の入力欄とエラー、ビートタブの退場理由の入力)。

- 2026-07-27 インスペクタのキャラタブで、**記憶の並び順を切り替えられる**ようにした(ユーザー提案)。見出しの右の「新 → 古 / 古 → 新」で反転する(記憶が 2 件以上あるときだけ出す)。**既定は新しい順** — 直近の変化が上に来て差分が見やすいため(fold の積み順は古い順なので、表示側で反転している)。選んだ順序は localStorage に保存する。

- 2026-07-27 構造モードで**矢印キーによるシーン移動**を追加(ユーザー提案)。**← → が時間軸(親 / 子)、↑ ↓ が同じ分岐点の兄弟レーン**で、DAG の 2 軸とキーの 2 軸を対応させている。子が複数あるとき(分岐)は正史の子へ進み、兄弟の並びは自動レイアウトと同じ順(正史の子が先、あとは作成順)なので画面の上下と ↑ ↓ が一致する。親を持たないシーン(島の根)どうしも ↑ ↓ で行き来できる。**選択ごと動かす**のでインスペクタも追従し、カメラは**ズームを保ったまま**そのシーンを中央に寄せる(`F` の fitBounds とは役割を分けた)。あわせて React Flow の `disableKeyboardA11y` を明示した — 既定では**フォーカス中のノードを矢印キーで動かす** a11y 操作が働いていて、シーン移動と衝突するため。入力欄(input / textarea / select / contenteditable)にフォーカスがあるときは従来どおり無効。

- 2026-07-27 動画を挿絵にしているシーンの**サムネイルを保存して使い回す**ようにした(ユーザー提案)。従来は構造モードのカードが動画ノードの数だけ `<video preload="metadata">` を並べていたため、モードを開くたびにデコードが走っていた(Chromium の同時動画要素数の上限にも当たる)。先頭付近(0.2 秒)のフレームを canvas で 1 枚切り出し、長辺 576px の JPEG としてアセットに保存して `nodes.thumb_path` に持ち、カードは `<img>` を出すだけにした。**既存データは構造モードを開いたときに 1 件ずつ埋まる**(サムネイルが無い間だけ従来どおり動画要素で先頭フレームを見せる)。生成は `src/renderer/src/videoThumb.ts` に集約し、デコードが重ならないよう 1 件ずつ実行する。作れなかった動画は記録して開くたびの再試行を避ける。挿絵を差し替え・取り外したときはサムネイルの参照も落とす。鑑賞モードは従来どおり動画を再生する(`CrossfadeLoopVideo`)。`gc_assets` の参照リストに `thumb_path` を追加(入れないと生成したサムネイルが 1 時間後に回収される)。

- 2026-07-27 構造モードで**範囲選択しているシーン数をステータスバーに表示**するようにした(ユーザー提案)。「3 シーンを選択中・右クリックで一括操作」。範囲選択の状態はこれまで右クリックして初めて分かる作りで、一括操作(イベント作り直し / 清書 / 切り離し / 削除)も隠れていたため、選択中だけヒントを添えている。**2 件以上のときだけ出す**(1 件はインスペクタが既にそのシーンを映しているので出さない)。モードを切り替えると `StructureMode` は破棄されるがステータスバーは生き続けるため、アンマウント時に 0 を通知して表示を残さない。

- 2026-07-27 「+ シーン」で追加したノードが、**選択ノードよりかなり左に置かれる**不具合を修正(ユーザー報告)。新しいノードは座標を持たないので自動レイアウトに載るが、自動レイアウトの x は「原点からの深さ × カード幅」で決まるため、ドラッグで動かしたノードや ⊕ で画面中央に置いた島の子は、親から遠く離れた場所に出ていた。**親が手動配置のときは自動レイアウトに任せず、親の右隣に置く**ようにした(既に子がいればその数だけ下のレーンへ)。親が自動配置のときは従来どおり自動レイアウトに任せる(全体が揃うため)。同じ理由で位置がずれていた「▶ 生成 / 分岐生成」と「⤵ 割り込み」にも適用(割り込みは元の後続シーンと重ならないよう 1 レーン下)。

- 2026-07-27 インスペクタのキャラタブの**好感度バーを中心 0 の発散バー**にした(ユーザー指摘)。従来は 0 が左端で、負の値だけ右寄せしていたため、+0.5 と −0.5 が同じ長さの棒として離れた位置に出ていた。中心に目盛りを置き、好意は右へ・反感は左へ伸ばす(±1.0 で端に届く)。

- 2026-07-27 「イベント抽出(LLM)」を積んだ後、**別のシーンに移ってもボタンが「待機中…」のままになる**不具合を修正。`EventsEditor` はシーンを切り替えても作り直されない(`node` prop が差し替わるだけ)ため、待機中フラグをローカル state に持つと次のシーンに引きずられていた。`tasks.ts` の `Task` に `kind` / `nodeId` を足し、`useTaskFor(kind, nodeId)` で**キューから状態を引く**方式に変更。同じ原因の 2 件も直った: 待機中のタスクをステータスバーから取り消してもボタンが戻らない(runner が走らないのでフラグが落ちない)、抽出結果と検証エラーが移動先のシーンのものとして表示される。あわせて、抽出対象のノード ID は積んだ時点で固定し、シーン切替時に検証結果・エラー・編集中の入力をリセットするようにした。手動編集の保存中フラグは抽出と分離し、抽出中はイベントの追加・編集・削除を止める(上書きされるため)。

- 2026-07-27 **場所(places)を登録制にした**(Step 0。設計は [docs/design/places.md](design/places.md))。動機は「現在のロケーションが曖昧」で、場所の情報が `nodes.location` の自由テキスト / `chars.facts.location` / `world.facts` の 3 系統に散っていたこと。
  - `places` テーブルを追加(`characters` と同型: 名前 / 説明 / 雰囲気 / 色)。**シーンは登録済みの場所を 1 つだけ選ぶ**。記憶も関係も持たない
  - 既存データは起動時に**一回限りの移行**で変換する(`nodes.location` の自由テキストを places に登録して ID 参照に置換。同じ文字列は 1 つにまとめる。`PRAGMA user_version` 1 → 2)
  - **実効ロケーション**: `location` が空欄なら親を遡って直近の場所を使う(`Store.effective_location`)。「場所が変わらない限り書かない」という書き方ができる。ノードカードでは引き継ぎ分を淡く表示し、インスペクタの選択肢も「引き継ぎ(港町)」と出す
  - 清書プロンプトに `## 場所`(説明 + 雰囲気)を追加。POV ではフィルタしない(その場にいる以上、場所の様子は見えている)。シーン生成にも「場所一覧」「現在の場所」を渡し、`location` は**登録済み ID の enum** にした(表記ゆれが構造的に消える。場所が未登録のときは location を出させない)
  - 相談チャットの `get_beats` / `get_state` も場所名を返す。`propose_beats` からは location を外した(空欄で挿入すればアンカーから引き継ぐ)
  - EVENT_RULES から `fact_set(scope="char", key="location")` の例示を削除(ノードの location と食い違う値を LLM が出す原因だった)
  - キャラクター庫を**資料庫**に改称し、「キャラクター / 場所」のタブにした
  - 場所の状態が時系列で変化する仕組み(`fact_set(scope="place")`)は Step 1 で、実際に変化するシーンを書いてから入れる

- 2026-07-27 校正のポップアップが選択中のテキストに重ならないようにした。textarea は内部の座標を返さないので、同じ幅・フォント・行間の隠し要素に同じテキストを流し込んで選択範囲の上端・下端を実測(ミラー方式)。通常は選択の最終行の下に出し、入りきらないときは選択の上へ回す。全文校正(選択なし)のときは従来どおり上端。
- 2026-07-27 キャラタブの記憶一覧に**どのシーンの記憶か**を表示(「← 裏切り」)。クリックでそのシーンへ移動でき、選択中のシーンで得た記憶はアクセント色 +「(このシーン)」で区別する。データは既に読み込んでいる全ノードのイベントから作るので API 追加なし。

- 2026-07-26 09:40 イベント欄を JSON 手書きから**型ごとのフォーム**に変更(`src/renderer/src/EventsEditor.tsx` として切り出し)。一覧は「記憶を追加 / アヤ: 「石橋でケンの裏切りを知った」(重要度 0.90 / 感情 -0.6)」のように人が読める要約で表示し、**✎ 編集**で開くとプルダウン(キャラ / 対象 / 種類)、スライダー + 数値入力(感情 / 重要度 / 変化量 / 関係値)、テキスト(内容 / 理由 / 関係の一言)に落ちる。fact_set のキーは既に使われているキーを入力候補に出し、退場理由も death / departure などの候補つき。**LLM が抽出したイベントも同じフォームで編集できる**(保存すると source は user になる)。種類の表示も日本語ラベル(`memory_add` → 「記憶を追加」)にした。

- 2026-07-26 09:15 単発の「イベント抽出(LLM)」もキューに積むようにした。他の処理が走っていても順番待ちになり(ボタンは「待機中…」→「処理中…」)、ステータスバーに並んで待機一覧から取り消せる。中止のため `POST /nodes/{id}/extract_events` に AbortSignal を渡せるようにした。これで LLM を使う操作はすべてキュー経由(生成 / 分岐生成 / イベント抽出 / チェーン再抽出 / 一括再抽出 / 清書 / 一括清書)。相談チャットの送信は対話なので対象外。

- 2026-07-26 09:00 「ここから最後まで」などの清書で、ステータスバーに何シーン中の何件目かが出ないのを修正。1 タスクの中で N シーンを順に書くため件数は押した時点では確定せず、フロントから `total` を渡せていなかった。清書ストリームの先頭に `{stage: "start", total: N}` を追加し、`skip_existing`(未清書のみ)の絞り込み後の実数を返すようにした。

- 2026-07-26 08:50 LLM を使う処理を**キュー化**した(`tasks.ts`)。llama-server は 1 リクエストずつしか処理しないので、積んだ順に**必ず 1 件ずつ**実行する。実行中でも「生成」「清書」を押せて、押した分だけ積まれる(生成の指示文は積んだ時点のものを使い、入力欄は即クリア)。ステータスバーは実行中のタスク(名前 / 何件目か / シーン名 / 経過 / ■ 中止)と **「待機 N ▴」**を表示し、待機一覧から個別に取り消せる(実行中は AbortSignal で中止、待機中はキューから削除)。キャンバス左上にあった再抽出・一括清書の進捗ピルはステータスバーと重複するので削除。進捗の数え方を「いま何件目か」に統一した(以前はピルが 1 から、ステータスバーが 0 から数えていて食い違っていた)。
- 2026-07-26 08:45 シーンエディタの Cast チップで、⊗ が出ない(cast に入っていない)キャラの右余白が詰まって見えていたのを修正(⊗ の有無で左右の余白を切り替え)。

- 2026-07-26 08:15 インスペクタに「**事実**」タブを追加(`src/renderer/src/FactTimeline.tsx`)。fact_set を**状態帯タイムライン**として描く: 横軸がパス上のシーン、縦がキーのレーンで、値が続く区間を帯で表示する(`location [白樺荘・玄関ホール ──][書斎 ────]` のように、いつからいつまでその状態かが見える)。対象はキャラごと / 世界(world facts)をセレクタで切替。帯やシーン見出しのクリックでそのシーンを選択。同じシーン内で上書きされた値は区間を持たないので描かない(後勝ち)。表示は選択シーンまでのパス(未選択なら正史)を対象に、イベントを直接読んで組む(fold はしない)。

- 2026-07-26 07:55 構造モードでシーンを選んだ状態で「鑑賞」を開くと、**そのシーンの位置から表示**するようにした(スクロール / 見開きモードはスクロール、ページモードはそのシーンを含む最初のページへジャンプ)。選択シーンは App が保持して鑑賞モードに渡す。正史外(分岐や島)のシーンは清書一覧に出ないため、その場合は先頭のまま。

- 2026-07-26 07:40 走っている処理を**下のステータスバーに集約**した(`src/renderer/src/tasks.ts` の小さなグローバルストア + `useSyncExternalStore`)。モードを切り替えるとページのコンポーネントは破棄されるが処理は続いているため、進捗が見えなくなっていた問題への対処。表示は「名前 + 進捗(N/M)+ いま処理しているシーン名 + 経過時間 + ■ 中止」で、**どのページからでも中止できる**。登録したのは 鑑賞モードの清書 / シーン生成・分岐生成 / イベントのチェーン再抽出 / 選択シーンの一括再抽出 / 一括清書。

- 2026-07-26 07:20 ノードが引き継ぐデータの一覧を [docs/design/node-inheritance.md](design/node-inheritance.md) にまとめた(引き継ぐ state / そのシーン限りの項目 / 導出される値 / 記憶と清書の引き継ぎ方 / 編集操作ごとの波及)。

- 2026-07-26 07:05 キャラクター編集ページのヘッダーを整理。プロフィール画像を 64px → **112px** に拡大(頭文字のフォールバックも 36px)。並びは「画像 → 画像を差し替え・画像を外す → 名前」の縦積みにし、名前は他の項目と同じ「ラベル + 入力欄」の形にした。キャラの色は名前の隣にラベル付きの項目として配置し、入力欄と同じ高さ(44px)に固定して縦のズレを解消。カラーピッカーは `-webkit-color-swatch` に border-radius を効かせて角を丸めた(`.color-swatch`、他の色入力にも使える)。

- 2026-07-26 06:45 キャラクター庫の左サイドバーを見やすく調整。アイコンを 24px → **40px**、名前を 13px → **15px** に拡大し(画像が無いキャラも同じ大きさの丸に頭文字を出す)、行の余白も広げた。あわせて**サイドバーの境界をドラッグで動かせる**ようにした(180〜520px、構造モードのインスペクタと同じ見た目のハンドル。幅は localStorage に保存)。

- 2026-07-26 06:30 校正をアプリ共通のテキストエリア部品に切り出した(`src/renderer/src/ProofreadTextarea.tsx`)。**テキストを選択すると右下に「✎ 校正」ボタンが出て**、押すとポップアップに校正結果がストリーミング表示され、「置換」で選択範囲だけを差し替える(反映後は「↩ 元に戻す」で校正前に戻せる)。校正プリセットの選択はポップアップ内に置き、localStorage でアプリ全体で共有する。高さの自動調整も部品側に内包した。シーン本文の従来の校正 UI(ラベル行のプリセット + 校正ボタン + プレビュー)はこの部品に置き換え、**キャラクター庫のプロフィール / 外見 / 口調でも同じ操作で校正できる**ようになった。

- 2026-07-26 06:10 キャラクター庫のプロフィール / 外見 / 口調のテキストエリアを、内容に合わせて高さが伸びる形にした(シーン本文と同じ方式。スクロールバーは出さず、`rows` は最小の高さとして機能する)。キャラ切替・入力・ウインドウ幅の変化で再計算する。

- 2026-07-26 06:00 アプリ終了時に llama-server を必ず止めるようにした(VRAM の解放)。従来はバックエンドのプロセスツリーを `taskkill /T` する経路でしか落ちておらず、**既存のバックエンドを再利用して起動した場合**(開発中の手動起動や前回セッションの残骸)は誰も落とさないため llama-server が残っていた。`before-quit` で一度終了を止め、`POST /llm/stop` を投げてから sidecar を kill する順に変更。外部起動の llama-server はバックエンド側のマネージャが管理外として扱うので、この経路でも止まらない(意図的)。
- 2026-07-26 05:50 シーンエディタの Cast の ⊗(このシーンで退場)が反応しない不具合を修正。理由の入力に `window.prompt` を使っていたが、Electron では無効化されているため何も起きずに終了していた。チャットの会話名変更と同じインライン入力(Enter 確定 / Escape 取消)に置き換えた。

- 2026-07-26 05:40 **登場と退場を LLM の判断から外した**(ユーザー提案)。
  - **登場**: `char_introduce` を廃止し、**ノードの cast から導出**する方式に変更(`fold(state, events, cast)` が cast のキャラを未登場なら初登場として作る)。同じ情報を二重に持つのをやめたので、重複した introduce・退場済みキャラの蘇生・島を繋いだときの掃除といった問題が**構造的に起きなくなった**。`_ensure_cast_introduced` と冗長 introduce のフィルタは削除。検証からも「char_introduce 前のキャラが cast に…」を削除。既存データの introduce イベントは無害だが、右クリックの「整合を取る」で掃除できる
  - **退場**: `char_retire` も LLM スキーマから外し、**作者が UI で設定**する。シーンエディタの Cast チップに **⊗**(このシーンで退場。理由を入力)と **⏎**(取り消し)を追加。退場済みキャラを後のシーンの cast に入れると従来どおり検証で警告が出る
  - LLM が発行できるのは `memory_add` / `relationship_update` / `fact_set` の 3 種だけになり、スキーマの枝が減って文法制約付き生成も軽くなった。EVENT_RULES からも 2 行削除
  - 12B 実機で確認: 「アヤは去っていく」という本文でも `char_retire` が出ず(以前は誤判定の実績あり)、登場状態は cast から導出されて検証もクリーン
  - `state_cache` の妥当性キー(`input_hash`)に cast を含めるようにした(cast を編集したら再 fold されるように)

- 2026-07-26 05:10 島を繋いだ直後の処理を「LLM で作り直す」から「**整合を取る(LLM なし)**」に変更。状態は fold が親から積み直すので**再抽出は本来不要**で、実害があるのは上流で登場済みのキャラへの `char_introduce` が残ること(退場済みキャラに当たると `retire_reason` が消えて「蘇生」する)だけ。そこで `POST /nodes/{id}/normalize_chain` を追加し、接続時に**重複した登場イベントの削除と検証だけ**を即時実行するようにした(親から順に処理するので、上流を掃除した結果が下流の判定に反映される)。LLM による作り直しは、関係値の delta や記憶の文面を新しい文脈に合わせたいときの**任意操作**として、右クリックメニューとインスペクタのボタンに残す(ラベルに「LLM」と明記)。右クリックメニューにも「整合を取る(LLM なし)」を追加。

- 2026-07-26 04:50 構造モードに**右クリックメニュー**を追加。範囲選択した複数シーンに対してまとめて実行できる:
  - **イベントを作り直す**(選択したシーンだけ)/ **この先も**(選択 + 下流すべて)。`POST /nodes/reextract`。抽出は親の状態を前提にするので、選択順がばらばらでも**ルートからの深さ順に並べ替えて逐次実行**する
  - **清書(未清書のみ)** / **清書し直す(上書き)**。条件は**鑑賞モードで選んだスタイルプリセットと POV をそのまま使う**(`reader_preset_id` / `reader_pov_char` は既に settings に保存されている)。メニューには使用する条件を表示する。`POST /render` に `node_ids` と `skip_existing` を追加し、清書は直前シーンの散文に接続するため正史順に並べ替えて逐次実行する
  - **まとめて切り離す** / **まとめて削除**(いずれも件数つきの確認ダイアログ)
  - 進捗は左上に表示し中止でき、処理中のシーンは枠が光る(単発の処理と同じ扱い)
  - 右クリックしたノードが選択に含まれていなければ、そのノード単体を対象にする

- 2026-07-26 04:20 LLM 処理中のノードの枠が時計まわりに光るようにした。`@property --sg-angle` で conic-gradient の角度をアニメーションさせ、mask で枠線部分だけを残す方式(`.node-busy-ring`)。点灯するのは **シーン生成中(続きを書く元のノード)** / **イベント抽出中のノード** / **チェーン再抽出でいま処理しているノード**(逐次実行なので常に1つずつ移動する)。処理の対象がどこかが一目で分かる。

- 2026-07-26 04:00 ツールバーに「⊕ 独立シーン」を追加。どこにも繋がらないシーンを**画面の中央**に作る(`POST /nodes` の `detached`)。正史は伸びず status は draft。作ったノードを選んで「+ シーン」で足していけば島がそのまま育ち、あとでハンドルのドラッグで本編に繋げる。中央配置は手動配置として保存するので、自動レイアウトに引っ張られない。従来の「+ シーン」(選択ノードの子 / 未選択なら正史の末尾)は変更なし。

- 2026-07-26 03:40 **ノードのエッジを切断・接続できるようにした**(まとまったエピソードを島として作り置きし、後から本編に繋ぐため)。
  - 切断: エッジをクリックして Delete、またはダブルクリック。そのシーン以下が独立した島になる(シーンは消えず、島の内部構造も保たれる。`POST /nodes/{id}/detach`)。島は正史から外れるので status は draft、story_order も貼り直す
  - 接続: ノードのハンドルをドラッグして親子を繋ぐ(`POST /edges`)。繋げるのは「島の根(親がいないノード)」だけで、多重親と循環はガードする。既定は draft で接続し、正史にするのは従来どおり「★ このブランチを正史にする」
  - 島は自分の根から fold されるので、繋ぐまでは上流の状態を引き継がない(単独で書けて、繋いだ瞬間に前提が変わる)
  - **チェーン再抽出**: 接続直後に「この先 N シーンのイベントを作り直しますか?」と聞く。インスペクタの「⟳ ここから先のイベントを作り直す」からも実行できる。親から子の順に逐次抽出し(各シーンは親の新しい状態を前提にするため)、進捗表示と中止に対応(`POST /nodes/{id}/reextract_chain`、SSE)
  - 再抽出は破壊的なので、**手動で足したイベント(source=user)は既定で残す**。抽出結果と同一内容のものは重複させない
  - 上流で既に登場済みのキャラへの `char_introduce` は抽出時に捨てるようにした(重複の解消と、退場済みキャラが「蘇生」する事故の防止)
  - 12B 実機で「本編2シーン + 島2シーンを接続 → チェーン再抽出」を確認

- 2026-07-26 03:00 チャット周りの絵文字をフラットな線画アイコンに統一(`src/renderer/src/icons.tsx` に最小セットを新設。lucide 準拠のパス、色は currentColor)。置き換えたのは 💬(チャットトグル / ヘッダー / トークン数)、🎭(キャラチャットの履歴項目)、🔍・💭(ツール実行・記憶をたどる)、✨(内容から生成された候補)、⚡・🕐・🔧(生成統計)。`<option>` には SVG を置けないため、相手セレクタの中だけは絵文字を外して文字のみにした。あわせて削除アイコン・削除メニューの赤(danger)をやめ、周囲と同じ落ち着いた色に。

- 2026-07-26 02:40 記憶(memory_add)に**目撃と伝聞**を含めるようにした。共通イベントルール(シーン生成 / イベント抽出で共用)に「行動した本人だけでなく、その場に居合わせて心が動いたキャラにも出す(見た・聞いた・気づいた、も記憶)」「人から聞いた話は誰から聞いたかを含めて、見たことと区別できる書き方にする」「importance の目安(人生を変える 0.8〜1.0 / 当事者の体験 0.5〜0.7 / 目撃・伝聞 0.3〜0.5 / 些細 0.1〜0.2)」を追加。従来は行為者にしか記憶が出ず、居合わせただけのキャラや伝聞が state に残らないため、キャラクターとの会話や POV 清書で本人が「知らない」と答えてしまっていた。12B 実機で、傍観者の目撃記憶(0.5)と伝聞記憶(0.4)が分かれて出ることを確認。

- 2026-07-26 02:20 感情の核(emotional_core)を少し長めに。自動生成の指示を「1行20字前後」→「40字前後(長くなるときは2文まで)」に変更し、スキーマの maxLength を 60 → 160(title は 30 → 40)に緩和した。maxLength は文字列の暴走ループ対策として短く設定していたが、原因が思考モード(全呼び出しで無効化済み)と判明したため。あわせてシーンエディタの Emotional core 欄を input から自動で高さが伸びる textarea に変更(シーン本文と同じ仕組み。折り返しと幅変更にも追従)。

- 2026-07-26 02:00 チャットのメッセージにホバー操作を追加(lm-chat の msg-action-btn を移植)。自分の発言には **✎ 編集 / ⟳ 再生成 / 🗑 削除**、返事には **🗑 削除**(発言は残す)。編集はその場が入力欄になり「送り直す」でその往復以降を作り直す(Escape で取消)。編集・再生成は `POST /chat/send` の新しい `replace_from` で履歴を巻き戻してから送る。削除は `DELETE /chats/{id}/turn/{index}`(`keep_user=true` で返事だけ削除)で、ツール行も往復の一部として一緒に消える。生成中はアイコンを出さない。往復の終わりに保存済み履歴から表示を組み直して位置を揃える。

- 2026-07-26 01:40 チャットの返事の下に生成統計を表示(lm-chat と同じ並び): `⚡ 38.3 tok/sec · 💬 10 tokens · 🕐 0.26s · Finish reason: stop`。ツールを使った返事は `🔧 N ステップ` も付き、トークン数と時間はステップの合計。llama-server の最終チャンクの `timings`(無ければ `usage`)から取得し、取れない項目は省略。統計は assistant メッセージの `meta` として保存するので履歴を読み直しても残る(LLM へ送るメッセージからは除外)。12B 実機で確認済み。

- 2026-07-26 01:20 チャットの会話一覧を左サイドバー(幅 208px)に変更。これまでの会話が更新の新しい順に並び、上部に「+ 新しい会話」、各項目は「見出し + アンカー名」の2行(キャラチャットは 🎭 付き、表示中はアクセント色)。項目ホバーで出る **⋯ メニュー**から**会話名の変更**(インライン入力。Enter 確定 / Escape 取消 / 空で既定の見出しに戻る)と**削除**ができる。これに伴いヘッダーの履歴プルダウン・削除ボタン・**全クリアボタンを廃止**。バックエンドは `chats.title` 列の追加(冪等 ALTER)と `PATCH /chats/{id}`、一覧に title / snippet / anchor_title / char_name を返す形。

- 2026-07-26 01:00 キャラクター庫の保存ボタンを、変更がないときは非アクティブ表示にした(シーンエディタと同じ判定方式。name / color / profile / appearance / voice の下書きと保存値の差分で判定)。プロフィール画像は切り抜き確定時にその場で保存されるため判定に含めない。

- 2026-07-26 00:45 キャラチャットにキャラクターアイコンを表示(プロフィール画像。無い場合はキャラ色 + 頭文字の丸で代替)。ヘッダーの相手セレクタ横(20px)、回答バブルの左(56px、ストリーミング中も)、会話前の空状態(56px)。あわせて、回答が終わるたびに履歴一覧を取り直すよう修正(以前はチャットを開いたときだけ取得しており、いま話したチャットが「履歴…」に出てこなかった。保存自体はできていた)。

- 2026-07-26 00:25 チャットに「キャラクターと話す」モードを追加。ヘッダーの相手セレクタでキャラを選ぶと、**アンカー時点のそのキャラ本人**と話せる(シーン12時点と30時点では人格の材料が違う)。システムプロンプトは fold 済み state から構築(プロフィール + facts + 関係の言語化 + importance 上位の記憶)。シーン一覧は渡さず、知識は記憶経由のみ — 「なぜ知ってるの?」という矛盾の検出器を兼ねる。ツールは `recall`(自分の記憶の検索)のみ。枠組みは「インタビュー」(既定、作者がキャラに質問)と「劇中会話」(見知らぬ相手として、本編には含まれない)をトグルで選択。スコープは常に upto 固定。キャラチャットは履歴に 🎭 付きで保存され、回答バブルはキャラ色の枠。質問候補チップもキャラ用の 6 件に差し替わる。12B 実機で E2E 確認済み(chats に char_id / mode 列を冪等 ALTER で追加)。

- 2026-07-25 23:52 相談チャットにコンテキスト使用量のドーナツリングを追加(lm-chat の token-ring を移植)。入力欄の右に 26px のリング + パーセントを出し、ホバーで「会話トークン / コンテキスト上限 / 使用中・残り」を表示する。70% でオレンジ、90% で赤。`POST /chat/token_usage` が「次のターンで送られる内容(システム + ツール定義 + 保存済み履歴)」を llama-server の `/tokenize` で数え、上限は設定の Context Length(`llm_ctx_size`)。LLM 未起動時は文字数からの概算(`≈` 付き)。更新は開いたとき / 新規 / 履歴読み込み / 回答直後。

- 2026-07-25 23:30 相談チャットの質問候補に「物語の内容から作られた質問」を追加(`POST /chat/suggest_questions` + `chat_agent.suggest_questions`)。ツールを使わない軽量な 1 回の呼び出しで、見えている範囲の末尾 12 シーンと直近 2 往復から 3 件を生成し、候補チップの下段に最大 2 件を ✨ 付きで並べる(固定の候補は上段に残る)。チャットを開いたとき・新規・履歴読み込み・回答直後に取り直す。設定に「相談チャット」セクションを新設し、`chat_dynamic_suggestions` でオン/オフできる(既定オン)。オフ・LLM 未起動・生成失敗はいずれも空を返し、固定の候補のまま(候補生成のために llama-server を自動起動はしない)。

- 2026-07-25 23:05 相談チャットに候補質問チップを追加(流れを要約 / 状態を要約 / 関係の変化 / 未回収の伏線 / 矛盾チェック / 展開を提案 の 6 件)。video-content-analyzer の qa-suggest-chip を参考に、**入力欄の上に常時表示・右寄せの縦積み**(ピル型、クリックで即送信)。狭いペインで会話が潰れないよう同時表示は 3 件で、送信ボタン隣の ⟳ で次の 3 件に巡回する(ランダムではなく決まった順)。相談チャット全体の設計は [docs/design/chat.md](design/chat.md) にまとめた。

- 2026-07-25 22:40 構造モードのキャンバス上の UI を整理。生成 UI(指示テキストエリア + 生成ボタン)を常時表示からツールバーの「▶ 生成」ボタンで開くポップオーバーに変更し、既定は畳んだ状態にした。生成を開始すると自動で畳み、代わりに進捗ピル(状況 + 経過秒 + ■ 中止)を出す(完了後は ✕ で消せる)。補助操作の「間に挿入」「自動整列」はアイコンのみ(⤵ / ⟲、説明は tooltip)に、「+ シーン追加」は「+ シーン」に短縮。相談チャットのトグル 💬 は左下の丸ボタンに移動(ミニマップの反対側)。

- 2026-07-25 22:28 インスペクタの幅をドラッグで変えたときも、ノードエリアが縮んだ分の半分だけ React Flow の視点を横にずらすようにした(チャットの高さ変更と同じ補正。補正処理は `shiftViewportForShrink` に統一)。ノードがインスペクタの裏に押し出されなくなる。

- 2026-07-25 22:20 相談チャットを完全な上下分割ペインに変更。常時表示だった下端のトグルバーを廃止し、ノードエリアのツールバーに 💬 アイコンを追加(押すと下段にチャットが開き、押している間はアクセント色。チャット側の「✕ 閉じる」でも閉じる)。開閉・高さ変更のたびに React Flow のビューポートを縮んだ分の半分だけ上げるようにしたので、ノードがチャットの裏に隠れて「上に重なって出た」ように見える現象がなくなった。分割線の見た目・当たり判定はインスペクタの分割線と共通(ドラッグで高さ変更、ノードエリアは 160px 以上残す)。

- 2026-07-25 22:03 ミニマップの表示/非表示を設定で切り替えられるようにした(設定に「構造モード」グループを追加、既定は表示)。設定ポップアップを閉じたタイミングで構造モードに反映される。

- 2026-07-25 22:01 ノードの流れを縦(上→下)から横(左→右)に変更。正史は左から右へ一直線、分岐は下のレーンに配置する。カード幅は固定なので x は深さで決まり、レーンの y はそのレーンの実測最大高さを積み上げて決める。ハンドルも上下から左右に変更。既存の手動配置はそのまま残るため、必要なら「⟲ 自動整列」でリセットする。

- 2026-07-25 21:55 相談チャットをノードエリアの下段に配置(右のインスペクタに被らないよう、キャンバスとチャットを左カラムにまとめた)。リサイズ上限もノードエリアを 160px 残す形に変更。

- 2026-07-25 21:50 相談チャットの高さを上端のドラッグで変更できるようにした(最小 220px〜画面の 85%、変更した高さは localStorage に保存)。既定の高さも 320px から 440px に拡大。

- 2026-07-25 21:48 途中のシーンも削除できるようにした(従来はリーフのみ)。削除は「抜き取り」方式で、子はすべて親に付け替わる(親 → 子が直結。連鎖エッジは is_canon を保つので正史は繋がったまま、分岐も構造を保って残る)。根を削除した場合は子がそれぞれ新しい根になる。削除後は status・state_cache・記憶の正史順を再同期。

- 2026-07-25 21:41 ノード選択中に Delete キーでシーンを削除できるようにした(確認ダイアログ付き)。あわせて React Flow 既定の Backspace 削除を無効化(API を通さず画面上だけノードが消え、バックエンドと不整合になっていたため)。

- 2026-07-25 21:35 シーンを既存の並びの間に割り込ませる「間に挿入」を追加。構造モードでノードを選択すると「⤵ 間に挿入」ボタンが出て、選択ノードと後続シーンの間に新しいシーンを挿入する(insert_node_after。分岐エッジは親に残し、下流の状態・記憶順を再同期)。

- 2026-07-25 15:50 思考モード(reasoning)を全 LLM 呼び出しで明示的に無効化。payload に chat_template_kwargs {enable_thinking: false} を付与し、シーン生成・提案・イベント抽出・清書・校正・相談チャットの処理時間を短縮。参照しないテンプレートでは無視されるため全モデルで安全。

- 2026-07-25 15:48 シーン編集中にノードを非選択にして再選択すると未保存の編集が消える不具合を修正。BeatTab のアンマウントで下書きが失われていたため、ノード ID ごとに未保存下書きをセッション内キャッシュへ一時退避し、再選択時に復元するようにした(保存成功・削除時は破棄)。

- 2026-07-25 15:33 スタイルプリセット(散文用システムプロンプト)のインポート/エクスポートを追加。鑑賞モードのプリセット操作の隣に「書き出し」(組み込み以外を JSON 化)/「読み込み」(JSON を新規プリセットとして取り込み、既存・組み込みを壊さない)を配置。バックエンド変更なし。ライブラリ選択ボタンのフォルダアイコンを絵文字からフラットな線画 SVG に変更。

- 2026-07-25 15:00 ライブラリメニューのポップアップが下段のモードバーに隠れて見切れる不具合を修正。上部バー(header)に z-30 のスタッキングコンテキストを与え、ヘッダー配下のドロップダウンを下段バー・本文より前面に表示するようにした。

- 2026-07-25 14:56 タイトル/感情の核の提案(/suggest/scene_meta)が「構造化出力が max_tokens(512) で打ち切られました」で失敗する不具合を修正。スキーマの文字列に maxLength(title 30 / emotional_core 60)と minLength を付与し、文法制約付き生成の文字列暴走ループを防止(文法が境界付き繰り返しに落ちる)。文法制約と相性の悪いモデルを読み込んだときに顕在化していた。

- 2026-07-25 14:39 モデル選択モーダルから実際にモデルをロードするように修正(選択→設定保存→llama-server を起動し直す /llm/load を追加)。モデルバーの赤ドットを廃止し、読み込み済みはアクセント枠+ロード中はスピナーで表現。上部バー右端の「backend」状態表示も削除(起動状態は本文側で示すため冗長)。

- 2026-07-25 14:33 モデル選択をポップアップ化(lm-graph 風)。上部バーのモデルバーを押すとモーダルでモデルを選択(名前・パラメータ規模・量子化・サイズ表示、現在のモデルをハイライト)。左下ステータスバーの LLM 表示は冗長なので削除。あわせて llama.cpp(llama-server)の自動ダウンロード/インストール機構を lm-graph から移植(backend/llama_installer.py)。設定画面でリリース/バリアント(CUDA/CPU等)を選んで進捗付きインストール、runtime/ 配下に配置し自動検出、CUDA は cudart も同梱。

- 2026-07-25 14:22 上部バーを再構成(lm-graph 風)。中央にモデル選択バー(models/ から選択、左に稼働状態ドット)、右端に歯車アイコンを配置し、設定を画面遷移からポップアップウインドウに変更。構造/キャラクター/鑑賞のモード切替は上部バー直下の2段目メニューバーに移動。「Story Graph」表記を削除。ライブラリメニューは左端に移動。

- 2026-07-25 14:11 相談チャットを改善: 回答のストリーミング表示(llm に tools 対応ストリーミングを追加し、delta を SSE で配信)、Markdown 描画(marked + DOMPurify、news-picker から移植。リンクは OS ブラウザで開く)、履歴の削除(表示中チャットの「削除」/ 全履歴の「全クリア」)。

- 2026-07-25 14:01 シーンエディタの保存ボタンを、変更がないときは非アクティブ表示にした(下書きと保存値の差分で判定)。

- 2026-07-25 13:53 assets/images 内の未参照ファイルを自動掃除する GC を追加(起動時・ライブラリ切替時に実行)。挿絵やプロフィール画像の差し替え・取り外し・ノード/キャラ削除で残っていた孤児ファイルを回収する。参照共有の可能性があるためその場削除ではなく GC 方式。直近1時間以内のファイルはアップロード直後の保護のため対象外。

- 2026-07-25 13:21 シーンの挿絵に動画(mp4 / webm、100MB まで)を添付できるように。動画はミュートでループ再生し、ループの継ぎ目はクロスディゾルブで接続(story-flow の実装を移植)。つなぎ秒数は設定ページ「鑑賞モード」で変更可能(デフォルト 0.5 秒、0 で通常ループ)。構造モードのグラフカードでは負荷を抑えるため先頭フレームのみ表示。

- 2026-07-25 13:11 鑑賞モード・ページモードの不具合を2件修正。(1) 他モードから鑑賞モードに移った直後にページ分割が一度も走らず機能しない(シーン読込前に本文エリアのサイズ監視が空振りしたまま再実行されなかった)。(2) ページ切替の瞬間に、タイプライター表示前の次ページ本文が一瞬見える(進行位置のリセットを描画前 useLayoutEffect に変更)。

- 2026-07-25 09:04 キャラのプロフィール画像とシーンの挿絵を追加(装飾専用、LLM には渡さない)。画像はライブラリ内 assets/images に保存。関係図アバター、cast チップ、ノードカード、鑑賞モードの挿絵表示。画像なしでも全機能が成り立つフォールバック付き。

- 2026-07-25 07:56 Phase 5(相談チャット)を実装。構造モード下部のドロワーで物語の状態について相談できるエージェント(ツールで状態・記憶・シーンを自分で調査)。スコープ制御(アンカーまで/全体)、展開の提案カード → draft ブランチ挿入、履歴のアンカー付き保存。

- 2026-07-25 02:20 cast に未登場キャラを入れると char_introduce を自動付与(退場済みキャラは対象外)。イベント発行ルールを強化(char_retire は永久退場のみ、fact_set の scope 使い分け、delta の目安)し、イベント抽出プロンプトにも適用。

- 2026-07-25 01:50 シーン生成プロンプトを設定画面で編集可能に(ルール部は自動追加)。プロンプトログ(直近30件の送信プロンプト・応答の確認)を設定画面に追加。

- 2026-07-25 00:35 Phase 4(鑑賞モード)を実装。シーケンシャルレンダリング(スライディングウィンドウ接続)、スタイルプリセット、POV 制約、部分レンダー + stale 自動化、ビート昇格(散文選択 → ビート追記 + イベント diff)、Markdown エクスポート。

- 2026-07-25 00:24 Phase 3(関係グラフ)を実装。インスペクタに「関係図」タブ: 有向グラフ描画、時間スクラブ、閾値フィルタ、エゴネットワーク、エッジ履歴ドリルダウン、d3-force 初回配置 + ピン留め保存。

- 2026-07-25 00:09 Phase 2(編集耐性)を実装。分岐 DAG + 正史切替、ブランチ生成(what-if、draft 挿入)、手動イベント編集(イベント追加/削除、キャラ状態からの編集)、記憶 retrieval(Ruri v3 + FTS5 + sqlite-vec、RRF × 重要度 × 物語内順序減衰)を生成コンテキストに組込み。

- 2026-07-24 23:53 ライブラリ方式を導入(ストーリーごとのフォルダに DB を分離、ヘッダーから選択/切替/最近使った一覧)。ノードエリアのホイール操作をズームに修正(中ボタンドラッグ=パンは維持)。sidecar の二重 spawn を修正。

- 2026-07-24 23:31 Phase 1 M4(生成)を実装、Phase 1 完了。llama-server マネージャ、ビート生成(JSON schema 構造化出力 + SSE + 検証リトライ)、手動ビートのイベント抽出、LLM 起動/停止 UI。Gemma4-12B で E2E 検証済み。

- 2026-07-24 22:56 Phase 1 M1〜M3 を実装。Electron + React + FastAPI sidecar の骨格、SQLite スキーマ、fold エンジン + state_cache + ルール検証(pytest 19件)、UI シェル(構造モード 3 ペイン / キャラクター庫 / 設定)。`npm run dev` で起動可能。
- 2026-07-24 22:38 計画ドキュメント(docs/plan/goals.md / plan.md / progress.md)を作成。Phase 1 実装を開始。
