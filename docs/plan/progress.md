# progress — 進捗と注意点

作成日時: 2026-07-24 22:38
更新日時: 2026-08-01 06:29

## 現在の状態

- **Phase 5(相談チャット)完了。** 残るは Phase 6(スケール対応)と小タスク。
- **ライブラリ方式を導入**(lm-graph 踏襲): ストーリーごとのフォルダに `story-graph.db` を置く。現在のライブラリと最近使ったライブラリは `%APPDATA%/story-graph/app.json`(Electron userData)に保存。ヘッダー右のドロップダウンで切替(切替時はレンダラをリロード)。デフォルトはリポジトリ内 `data/`。
- `npm run dev` で Electron が起動し、FastAPI sidecar(ポート 8765〜自動探索)が自動 spawn される。
- バックエンドは単体でも起動可能: `cd backend && ../.venv/Scripts/python.exe -m uvicorn app:app --port 8765`
- テスト: `cd backend && ../.venv/Scripts/python.exe -m pytest tests/ -q`(127件、全て成功)

## 完了済み

- [x] 仕様確定: [docs/story-graph-spec.md](../story-graph-spec.md)
- [x] 移植元 3 リポジトリの調査(lm-graph / lm-chat / news-picker。結果は [plan.md](plan.md) の移植元マップ)
- [x] モデル配置: `models/gemma-4-31B-it-GGUF/`(Q6_K + mmproj)、`models/gemma-4-12B-it-GGUF/`(Q6_K + mmproj)
- [x] 計画ドキュメント作成(goals / plan / progress)
- [x] **M1 — 骨格**: electron-vite + React 19 + Tailwind、lm-graph のデザイントークン移植(`src/renderer/src/index.css`)、`.venv`(Python 3.13) + FastAPI `/health`、Electron main からの sidecar spawn + ヘルスチェック(`src/main/index.ts`)
- [x] **M2 — データ層**: SQLite スキーマ全テーブル(`backend/db.py`)、characters / factions CRUD、nodes / edges(単線タイムライン)、fold エンジン(`backend/fold.py`、純粋関数)、state_cache(input_hash / dirty 伝播 / 遅延再fold)、ルールベース検証(`backend/validation.py`)、memories 行の events からの再構築。pytest 19件
- [x] **M3 — UI(基本形)**: シェル(ヘッダー + 4 モード切替、`App.tsx`)、構造モード(React Flow 縦タイムライン + インスペクタ[ビート編集 / キャラ状態閲覧] + 相談チャットドロワー枠、`modes/StructureMode.tsx`)、キャラクター庫 CRUD(`modes/CharactersMode.tsx`)、設定画面(`modes/SettingsMode.tsx`)
- [x] **M4 — 生成**: llama-server マネージャ(`backend/llama_manager.py`、外部起動優先 + 自動 spawn)、LLM クライアント(`backend/llm.py`、httpx 非同期 + json_schema response_format)、生成パイプライン(`backend/generation.py`、コンテキスト構築 + ビート生成 SSE + 検証リトライ + イベント抽出)、UI(生成パネル / イベント抽出ボタン / LLM 起動・停止)。**12B で E2E 検証済み**(2連続ビート生成 → fold 状態確認 → 手動ビートのイベント抽出)

- [x] **Phase 2 — 編集耐性**(2026-07-25):
  - 分岐 DAG: 任意ノードからの分岐作成(`parent_id`)、`path_to` による分岐パスの fold(分岐点まで state 共有)、正史切替(`make_canon`: canon エッジ付け替え + status 導出 + story_order 再同期)、リーフ削除、`GET /graph`
  - ブランチ生成: `POST /generate/beat` に `parent_id`(what-if 指示付き、draft として挿入)
  - 手動イベント編集 UI: イベントの削除/追加(型テンプレート付き)、キャラタブから facts / relationship_set / memory_add を手動イベント化(source=user バッジ)
  - 記憶 retrieval: `backend/embed.py`(Ruri v3-310m、非対称プレフィックス、CPU)+ `backend/retrieval.py`(FTS5 trigram + sqlite-vec cosine の RRF × 重要度 × story_order 半減期減衰)。生成コンテキストに cast 各キャラの想起記憶 top5 を注入(spec §6.1-2)。意味検索の実動作を確認済み
  - UI: DAG レイアウト(正史=縦一直線、分岐=右レーンへ)、canon/draft のエッジ・ノード描画分け、「このブランチを正史にする」

- [x] **Phase 3 — 関係グラフ**(2026-07-25): インスペクタ「関係図」タブ(`src/renderer/src/RelationGraph.tsx`)。有向グラフ(色=感情価 / 太さ=|score| / 矢印=方向)、正史パスの時間スクラブ、|score| 閾値フィルタ、ノードクリックでエゴネットワーク、エッジクリックで reasons のイベント履歴(delta/理由/ノード)、初回 d3-force 配置 → `characters.graph_x/y` にピン留め保存(ドラッグで再配置可、新キャラのみ追加配置)

- [x] **Phase 4 — 鑑賞モード**(2026-07-25): `backend/rendering.py` + `modes/ReaderMode.tsx`。シーケンシャルレンダリング(直前散文の末尾 ~1400 字をスライディングウィンドウで接続、12B 実機で接続を確認)、スタイルプリセット(seed 2 種 + CRUD API)、POV(pov キャラの state にある情報のみをプロンプトへ)、部分レンダー(「このシーンのみ」「ここから最後まで」、上流編集で renders.stale 自動化)、ビート昇格(散文の選択 → LLM が追記案+イベント diff 案 → 確認して正史へ。**使われないため 2026-07-28 に削除**)、Markdown エクスポート、SSE トークンストリーミング表示

- [x] **Phase 5 — 相談チャット**(2026-07-25): `backend/chat_agent.py` + `src/renderer/src/ChatDrawer.tsx`。
  tool calling ループ(news-picker 方式、MAX_TOOL_STEPS=8、上限到達時は打ち切らずまとめさせる)。
  ツール: get_beats / get_state / search_memories(読み取り専用)+ propose_beats(提案カード)。
  スコープ upto(アンカーのパスまでしか見えない。チャット作成時に固定)/ all。
  提案カード → 「⑂ ブランチとして挿入」で anchor から draft 挿入。履歴はアンカー付きで chats に保存。
  llama-server に `--jinja` を追加(tool calling に必要)。12B 実機 E2E でツール使用・提案とも確認済み。
  UI 配置は「ノードエリアの下段の分割ペイン」。開閉トグルは左下の 💬 丸ボタンで、
  開閉と高さは `StructureMode` 側が持ち(`ChatDrawer` は `open` / `onClose` を受ける制御コンポーネント)、
  高さが変わるたびに React Flow のビューポートを差分の半分だけ動かして表示中の内容を保つ。
  追加済みの拡張(2026-07-26): 候補質問チップ(固定6件 + 内容から生成、設定でオン/オフ)、
  コンテキスト使用量リング(lm-chat 移植)、**キャラクターと話すモード**(アンカー時点のキャラ本人と
  インタビュー / 劇中会話。ツールは recall のみ。詳細は [docs/design/chat.md](../design/chat.md))、
  会話サイドバー(会話名の編集 / 削除)、メッセージのホバー操作(編集 / 再生成 / 削除)、生成統計の表示

- [x] **2026-07-26〜27 の作業**(上記以外。詳細は [changelog](../changelog.md)):
  - **登場は cast から導出、退場は作者が UI で設定**(LLM のイベントは 3 種のみに)。
    設計の背景は [node-inheritance.md](../design/node-inheritance.md)
  - **記憶に目撃と伝聞を含めるルール**(EVENT_RULES に 3 行 + importance の目安)
  - **LLM 処理のキュー化**(`tasks.ts`。1 件ずつ逐次、ステータスバーに実行中 + 待機 N、
    待機は個別に取り消し可)。処理中のノードは枠が時計まわりに光る
  - **右クリックの一括操作**(イベント作り直し / 清書 / 切り離し / 削除)。
    抽出は深さ順、清書は正史順に並べ替えて逐次実行
  - **イベント欄のフォーム化**(`EventsEditor.tsx`。JSON 手書きをやめ、型ごとのプルダウンと
    スライダーに。LLM 抽出結果も同じフォームで編集)
  - **事実タブ(状態帯タイムライン)**(`FactTimeline.tsx`)
  - **校正の共通部品化**(`ProofreadTextarea.tsx`。選択 → ✎ 校正 → ポップアップ → 置換。
    シーン本文とキャラクター庫で共用)。ポップアップは選択範囲に重ならない位置に出す
    (ミラー要素で選択の上端・下端を実測し、下に出す / 入らなければ上へ回す)
  - **記憶の出所シーン表示**(キャラタブの記憶一覧に「← シーン名」。クリックでそのシーンへ移動、
    選択中のシーンで得た記憶はアクセント色 +「(このシーン)」。API 追加なし)
  - アプリ終了時に llama-server を確実に停止(VRAM 解放)、UI の細かい調整多数

- [x] **場所(places)の登録 — Step 0**(2026-07-27。設計: [places.md](../design/places.md)):
  場所をキャラクターと同型の登録制エンティティにし、シーンは登録済みの場所を 1 つだけ選ぶ。
  `places` テーブル + CRUD API + 資料庫の「場所」タブ、`nodes.location` の ID 化と
  一回限りの移行(`user_version` 1 → 2)、**実効ロケーション**(空欄なら親を遡る。
  旧「location の引き継ぎ」タスク)、清書への `## 場所` 注入、生成の `location` を
  登録済み ID の enum 化、相談チャットの場所名表示。キャラクター庫 → **資料庫**に改称。
  pytest 107 件

- [x] **構造モードの清書タブ**(2026-07-29): インスペクタに「清書」タブを追加。選択シーンの散文を
  その場で見て書き直せるプレビューで、鑑賞モードと同じ `renders` を共有する。
  スタイルプリセット / POV / 本文フォントの選択は
  [RenderStyle.tsx](../../src/renderer/src/RenderStyle.tsx)(`useRenderStyle` +
  `RenderStyleControls`)に切り出して**鑑賞モードと共通化**した(保存先は従来どおり
  ライブラリごとの settings の `reader_*`)。右クリックの一括清書もこの条件を使う。
  バックエンドは単一シーン用に `GET /renders/{node_id}` を追加(`GET /renders` は正史限定なので、
  分岐・島のシーンが取れない)。**本文の直接編集は付けていない**(鑑賞モードと同じく読み取り専用。
  文章を直したいときはシーン本文を直して清書し直す)

- [x] **清書の分量指定**(2026-07-29): 目安の字数(おまかせ / 短め ~600 / 標準 ~1200 / 長め ~2000 /
  字数を指定)。プロンプトには目安の ±25% を幅として渡し、`max_tokens` も字数から算出する
  (`rendering.length_instruction` / `_max_tokens`。従来は 4096 固定で、長い指定だと切れていた)。
  **2 段構え**: 共通の既定値(settings の `reader_target_chars`。鑑賞モードのバーで変える)と、
  **シーン個別の指定**(`nodes.target_chars`。構造モードの清書タブで変える)。
  解決はサーバー側(`rendering.effective_target_chars`)なので、全編清書・一括清書でも
  シーンごとに書き分かる。保存は `POST /nodes/{node_id}/target_chars` で
  **state を汚さず既存の清書も stale にしない**(分量は fold に無関係)

- [x] **自動ロードのモデル選択バーへの反映**(2026-07-29): 生成が `ensure_running` で
  llama-server を spawn したとき、バーが「モデルをロードしてください」のまま沈黙していた。
  `GET /llm/status` に `loading`(spawn 済み & 未応答)を足し、バーは自分でロードしたときと
  同じ見た目(スピナー + リング + モデル名)にする。ポーリングは自己再帰の `setTimeout` にして
  読み込み中だけ 1.5 秒間隔に詰める

- [x] **全体コードレビューと不具合修正**(2026-07-29): バックエンド 22 件 + フロント 31 件を修正
  (詳細は [changelog](../changelog.md))。主なもの: 分岐の根の削除で正史エッジが二重になる /
  エラー応答時に半端な書き込みが残る(app.py にロールバックハンドラ追加)/ 場所編集で清書が
  stale にならない / チャット履歴の途中保存 / putEvents 全件置換のレース / タスク完了通知の
  stale closure(tasks.ts の `notifyGraphChanged` / `setNodeBusy` に集約)/ llama-server の
  二重起動・停止漏れ / embed ロード中の全 API フリーズ / 検索候補の押し出し。
  回帰テスト 5 件追加(pytest 118 件)

- [x] **スナップショット(バックアップ)**(2026-07-30。設計: [snapshots.md](../design/snapshots.md)):
  undo の代わりの「時点に戻す」。`backend/snapshots.py` + 設定画面の「バックアップ」セクション。
  危険な操作(削除 / 正史切替 / イベント編集・作り直し / つなぎ替え)の前に自動保存
  (契機ごとにスロットル、自動分は直近 30 件)、手動保存は無期限。復元は DB ファイル
  差し替え + 再接続 + レンダラ reload。復元直前も「復元の前」として自動保存されるので
  復元自体を取り消せる。gc_assets はスナップショットの参照画像も保護。
  **将来の LLM 書き込みツール(チャットからのプロット・キャラ操作)の安全網の前提**

## 未完了

- [ ] **章グループ(エピソード単位のまとめと stale 遮断)**(2026-08-01 ユーザー発案。
  設計メモ: [docs/design/chapters.md](../design/chapters.md)): ノードを章の枠でまとめ、
  章境界に「章をまとめた要約の記憶」(memory_compress ベースの digest)を置いて、
  章内の編集を章内に閉じる。構造モードは章ノードの中に入るドリルダウン型の
  階層表示(章ビュー ⇄ 章内ビュー。データモデルはシーンエッジ + groups ラベルのまま)、
  鑑賞モードの章見出し表示も含む。
  フェーズ A(stale 粒度改善)→ B(章の構造と表示)→ C(章じまいのまとめ。
  Phase 6 の memory_compress 自動化を兼ねる)の順。C の前に
  「イベント ID が置換で再発行される」課題(下記)の修正が前提
  - [x] **フェーズ A 完了**(2026-08-01): update_node の契機分離(プロンプト文面のみの
    編集は自シーンの清書だけ stale)+ cast 編集の early cutoff(状態ハッシュが
    変わったときだけ下流へ波及)。イベント編集は ID 再発行の課題があるため従来どおり
    全波及(cutoff は C の前提作業とあわせて設計)
- [ ] **チャットからの物語操作(LLM 書き込みツール)**(2026-07-29 ユーザー発案。構想段階):
  チャット経由でキャラ・場所・シーンを LLM が操作できるようにする。方針は
  propose_beats と同じ**提案カード方式**(LLM は変更案を積み、作者が適用)。段階案:
  ① キャラ・場所の提案カード(追加のみ)→ ② アンカーなしの構想モード + プロット md
  (正史ではなく材料)→ ③ プロット → draft の島シーン列を一括生成 → ④ 編集・組み換え
  ツール(差分プレビュー付き)。スナップショット実装済みで ④ の前提は満たした。
  着手時は docs/design/ に設計メモを書いてから
- [ ] **レビューで見送った既知の課題**(2026-07-29。いずれも設計判断が要るため未着手):
  - **イベント ID が置換で再発行される**: `EventIn` に id が無く、編集・正規化・再抽出のたびに
    全イベント ID が変わる。`memory_compress.replaces` や関係の reasons のノード間参照が
    宙に浮く(memory_compress は Phase 6 で本格導入予定なので、その設計時に id 引き継ぎを入れる)
  - **正史の根が「created_at 最古のルート」で決まる**: 本編の根を削除・切り離ししたとき、
    より古い島の根があると正史がそちらへ飛ぶ(`canon_path`)。正史の根を明示的に持つか要検討
  - **sidecar が強制 kill されたときの llama-server 孤児化**: shutdown が走らない経路では
    VRAM を掴んだまま残る。Windows Job Object で紐付けるのが本筋
  - **SSE ストリーム進行中のライブラリ切替**: 清書・再抽出の途中で切り替えると、以降の書き込みが
    新しいライブラリの DB に落ちる(store.conn 参照経由)。ストリーム開始時の root を控えて
    不一致なら中断するのが安い
  - **埋め込み encode は依然イベントループ上**: ロード中のフリーズは解消済み(trylock)だが、
    ロード後も 1 クエリごとに数十〜数百 ms ブロックする。to_thread 化は SQLite 接続の
    スレッド越えと「書き込みはループ上で直列」の前提に触るので、遅さが気になったら設計して入れる
- [ ] **Phase 6 — スケール対応**: memory_compress(記憶の自動要約圧縮)、LLM 検証パス(感情の一貫性、温度0.1)、faction フォールバック + factions UI、2ノード間差分表示、エクスポート強化
- [ ] **フル版の関係図**(2026-07-25 ユーザー発案): インスペクタ内のコンパクト版とは別に、
  しっかりしたバージョンを作る。
  - [x] **Step 1**(2026-07-28): 置き場所は資料庫の「関係図」タブに決定(ユーザー指定)。
    大きなキャンバス(880×620)+ 時間スクラブ + **直角折れ線ルーティング**
    (`RelationGraph.tsx` にグリッド A* を実装。ノードの周囲を通行止めにし、曲がりと
    通過済みセルにコストを付けて直進優先・並走レーン分離)。サイドバーのキャラクリックで
    エゴネットワーク切替(`egoCharId` を controlled prop 化)。コンパクト版と座標
    (`characters.graph_x/y`)は共有。フル版は**登場済みキャラを全員表示**
    (`showAllChars`。関係ゼロでも孤立ノードとして置く。しきい値は線のみ減らす)
  - [ ] より詳細な情報表示: キャラ選択で facts / 記憶 / 関係一覧のサイドパネル、エッジ履歴のタイムライン表示、
    faction 表示、2ノード間差分(Phase 6 と統合)、再生アニメーション? など
  - インスペクタ版は「選択ノード時点のクイックビュー」として残す(直線エッジのまま)
- [x] **エッジの切断・接続と島**(2026-07-26): エッジをクリック + Delete / ダブルクリックで切断、
  ハンドルのドラッグで接続(島の根のみ、多重親と循環はガード)。島は自分の根から fold されるので
  単独で書ける。
  **接続時に LLM は使わない**(2026-07-26 ユーザー指摘で方針変更): 状態は fold が親から積み直すため
  再抽出は不要で、実害は上流で登場済みのキャラへの `char_introduce` が残ることだけ
  (退場済みキャラに当たると蘇生する)。接続直後は `normalize_chain` で重複した登場イベントの削除と
  検証のみを即時実行する。LLM でのイベント再抽出は、delta や記憶の文面を新しい文脈に合わせたい
  ときの任意操作(右クリックメニュー / インスペクタのボタン。手動イベントは残す)
- [x] **「誰が何を知っているか」の境界 — (1) は解決**(2026-07-29 実装。方針 (a) を採用):
  **world facts = 「誰もが知っている公のこと」と規約で確定**(天気・季節・世相など)。
  秘密は知っているキャラの char fact に置く(`知っていること=真犯人は執事`)。実装は 3 点:
  ① LLM の抽出スキーマから `scope="world"` を削除(`generation._event_schemas`。
  world facts は作者の手動イベント専用に)+ EVENT_RULES を秘密の書き方込みで更新、
  ② キャラチャットは world facts を「(世間で知られていること)」として渡す(公なので正当)、
  ③ POV 清書も world facts を出すように変更(公なので視点キャラも知っている)。
  pytest にスキーマの回帰テスト追加(119 件)。
  下記 (2) 「本人の facts も知識とは別軸」は未解決のまま(必要になったら (b) 可視性フィールド)
- [ ] **「誰が何を知っているか」の境界**(2026-07-27 ユーザー指摘。経緯の記録):
  記憶は `memory_add` でキャラ単位に閉じているが、**facts には「誰が知っているか」の次元がない**。
  そのため知らないはずのことを知っている状態が作れてしまう。2 つの面がある。
  - **(1) world facts の素通し(具体的な漏れ)**: キャラチャットの
    `build_character_system`([backend/chat_agent.py](../../backend/chat_agent.py))が
    `state["world"]["facts"]` を丸ごと「(世界)〜」として本人のシステムプロンプトに入れている。
    `真犯人=執事` のようなものが world facts にあると、本人は知らないはずなのに知っていて、
    「なぜ知っているか」を聞かれると作り話をする。**POV 清書は対策済み**
    (`rendering._state_summary` は pov 指定時に world facts を出さない)で、キャラチャットだけが
    素通しという食い違いになっている
  - **(2) 本人の facts も知識とは別軸**: `毒に侵されている`(本人は気づいていない)のように、
    自分の facts でも本人が知らないことがある。facts は客観的な現在値なので、
    知っているかどうかは本来別の軸
  - 解き方の候補:
    - **(a) 規約で解く(スキーマ変更なし)**: `scope="world"` を「**誰もが知っている公のこと**」と
      定義する(天気・日付・世相)。秘密は知っているキャラの char fact に置く
      (`aya.知っていること=真犯人は執事`)。EVENT_RULES に 1 行 + キャラチャットの修正で済む。
      難点は**まだ誰も知らない客観的真実**(読者にも伏せる真相)の置き場所がなくなること
    - **(b) facts に可視性を持たせる**: `fact_set` に `known_by: [char_id]` か `secret: true` を
      任意で足し、キャラチャットと POV 清書がそれで絞る。波及は
      スキーマ・fold・手動編集 UI・プロンプト 3 か所([[記憶の入手経路フィールド]]と同規模)
  - **判断(2026-07-27)**: まず (a) + キャラチャットの漏れを塞ぐのが良さそう。実際に困るのは
    「本人が知らない world fact を語る」ことで、そこは規約とプロンプト修正で消える。
    (b) は「秘密の真実を state に持ちたい」と実際に思ったときで間に合う
- [ ] **知覚していない cast(気絶・睡眠)**(2026-07-27 ユーザー発案。上の項目と同じ主題):
  気を失っている / 眠っているキャラも、その場に倒れているので **cast には入れたい**(清書で描写される)。
  しかし cast にいると EVENT_RULES の「その場に居合わせて心が動いたキャラにも記憶を出す」が働き、
  **知らないはずの出来事が記憶として付く**。付いてしまうとキャラチャットがそれを語り出す。
  - 現状の回避: `fact_set(scope="char", key="意識", value="気絶中")` を置くとプロンプトには載るが、
    **強制力はない**(validation は memory_add の付与先を見ていない)。抽出後に手で消すのが確実
  - 構造で解くなら: `cast` が「**居る**人」と「**知覚した**人」を兼ねているのが根っこなので、
    ノードに「このシーンを知覚していない cast」を持たせる(退場と同じく作者が UI で指定)。
    そうすると生成プロンプトで明示でき、**検証で `memory_add` を弾ける / normalize で落とせる**。
    規模は小さい(`nodes` に列 1 つ、Cast チップにトグル、EVENT_RULES 1 行、validation 1 チェック)
  - **判断(2026-07-27)**: まず facts に `意識=気絶中` を置いて 12B の挙動を見る。
    実際に記憶が付いてしまうなら構造側に入れる
- [ ] **記憶の入手経路フィールド**(2026-07-26 検討。プロンプト対応は実施済み): `memory_add` に
  `source: "experience" | "witness" | "told"`(体験 / 目撃 / 伝聞)を任意フィールドとして足すか。
  - 背景: 目撃・伝聞を記憶に含める指示は EVENT_RULES に入れた(効果は 12B で確認済み)。
    現状は本文の書き方だけで区別しているので、機械的な判定ができない
  - 入れると嬉しいこと: キャラチャットで「なぜ知っているか」を明示できる / 伝聞は誤情報でありうる
    という扱い(嘘の伝聞・勘違い)を仕組みとして持てる / POV 清書で「又聞き」の描写を制御できる /
    UI で記憶にバッジを出せる
  - 波及範囲: 生成スキーマ・抽出スキーマ・fold・memories テーブル(列追加)・記憶の手動編集 UI・
    キャラチャットのシステムプロンプト。`told` の場合は情報源(誰から)も持たせるか要検討
  - 判断: しばらく本文だけの運用で使ってみて、「見た/聞いたの区別が弱い」と感じたら着手する
- [ ] **場所(places)の時系列変化 — Step 1 / 2**(2026-07-27 ユーザー発案。設計メモ:
  [places.md](../design/places.md))。**Step 0 は実装済み**(下記「完了済み」)。
  - Step 1: state に `places[id].facts` を追加、fold と `input_hash`(location を含める)、
    `fact_set(scope="place")` を**手動イベント UI からのみ**、インスペクタの場所タブ
  - Step 2(効果を見てから): LLM の抽出スキーマに `scope="place"` を開放
  - **着手の判断**: 場所が実際に変化するシーン(橋が落ちる等)を書きたくなったときに始める。
    それまでは Step 0 の「登録された場所の固定設定が清書に渡る」だけで足りる(2026-07-27 の判断)
- [ ] **資料庫の勢力(factions)タブ**: API は実装済みで UI だけ無い。資料庫がタブ構造になったので、
  キャラクター / 場所と同じ形で 3 つめのタブとして足せる
- [ ] **facts 可視化の続き**(2026-07-26 発案。状態帯タイムラインは実装済み):
  - 各 fact に「いつから」を表示(インスペクタのキャラタブ。値を立てたイベントのノードを遡って
    「第3話から」と出し、クリックでそのシーンへ)。実装は軽く、情報価値が高い。
    記憶側の同じ表示(出所シーン + クリックで移動)は 2026-07-27 に実装済みなので、それに揃える
  - 場所マップ: `location` の値ごとにキャラをまとめて表示(「石橋: アヤ ケン / 港: リン」)。
    時間スクラブと組み合わせると人の移動が見える
  - ノードカードに変化バッジ: そのシーンで変わった facts を 1〜2 個だけ小さく出す(`location→港`)
- [ ] **emotion の活用**(2026-07-26 発案): `memory_add.emotion`(-1〜+1)は記録しているが、
  現在どこからも参照していない。使い道は ①記憶の想起スコアに混ぜる(強い感情の記憶を優先)
  ②Phase 6 の LLM 検証パスで「関係値の変化と感情の符号が矛盾していないか」を見る
  ③UI で記憶の色分け。まず ① を試すのが安い
- [ ] **清書をシーン単位でキューに積む**(2026-07-26 発案): いま「ここから最後まで」は 1 タスクで
  内部的に N シーンを回している(進捗は N/M で見える)。シーンごとに 1 タスクにすると、
  待機一覧に並んで**途中のシーンだけ取り消せる**。清書は直前シーンの散文に接続するが、
  キューは逐次実行なので順序は保たれる
- [ ] **キャラの種類が人以外のときの文言**(2026-07-26 検討 → 当面不要と判断): データ構造は種を
  問わない(犬でも関係値・記憶・facts が使える)。ただしキャラチャットのシステムプロンプトが
  「あなたは『○○』という**人物**です」、清書が `## 登場人物` と人間前提。犬を「心の声」で
  喋らせる運用なら実害は小さいので保留。気になったら文言の中立化 + 関係ラベルの例に
  人以外を混ぜる(`飼い主として慕う` など)
- [ ] 生成コンテキストの絞り込み(spec §6.1 準拠: cast 相互 + オフスクリーン上位3件。現状は全キャラ・全状態を入れており、キャラが増えると肥大化する)
- [ ] 残タスク(小): factions の UI(API のみ実装済み)、31B での動作確認、生成イベントの重複除去(12B は同一イベントを重複して出すことがある)、`GET /graph` の全ノード events 込み返却が大規模ストーリーで重くなったら分割
- [ ] 鑑賞モードの縦書き表示(2026-07-25 ユーザー発案。任意): CSS `writing-mode: vertical-rl` 自体は Chromium で動くが、ページ分割の実測を縦横入れ替える(高さ→幅の二分探索)必要がある。ページモードに追加する場合は右→左のページ送りも要検討

## 注意点

- **用語(2026-07-25 ユーザー決定)**: UI 表示は「ビート」→**「シーン」**、レンダー散文→**「清書」**。コード・DB スキーマ・spec・LLM プロンプト内は `beat` / `render` のまま変えない(表示ラベルのみ)。
- **Python は必ず `.venv` を使う**(ユーザー指示)。
- spec §12 の「Electron + FastAPI 起動骨格 ← lm-graph」は実態と異なる: lm-graph に Python バックエンドはない。FastAPI 構成の実例は news-picker / lm-chat 側。lm-graph からは spawn+ヘルスチェックのロジックだけ借りた。
- 検索層の移植(Phase 2)は lm-chat 直接ではなく news-picker 版を雛形にする(lm-chat には Ruri v3 の非対称プレフィックス欠落バグがあり、news-picker で修正済み)。
- llama-server バイナリはまだ本リポジトリに置いていない。lm-graph の `bin/llama-server/b9496-win-cuda13-x64` を流用予定。パスは settings で上書き可能にする。
- モデルは spec 目安(Q5)より上の Q6_K。VRAM が厳しければ 12B に切替。
- Windows の `python` コマンドは Store スタブなので venv 作成は `py -3.13` を使う。
- electron-vite dev では `app.getAppPath()` が `out/main` を返す。リポジトリルート解決は `.venv` の存在チェックで行っている(`src/main/index.ts` の `repoRoot`)。
- sidecar の起動は `ensureSidecar()`(in-flight Promise)で一本化している。`whenReady` と `bootstrap` IPC が同時に呼んでも spawn は 1 回。Windows は占有ポートでも listen チェックをすり抜けることがあるため、spawn 前に必ず既存ポートの `/health` を確認して healthy なら再利用する。
- バックエンドのライブラリ切替は `Store.switch_library`(接続差し替え・インスタンス再利用)。起動時は `STORY_GRAPH_LIBRARY` 環境変数(Electron が app.json から渡す)で解決。レンダラは起動時に app.json とバックエンドのライブラリが一致しているか確認して同期する(再利用 sidecar 対策)。
- bat ファイルに日本語を書かない(UTF-8 の日本語バイト列が cp932 パースで後続コマンドを壊す)。メッセージは ASCII のみ。
- FastAPI のエンドポイントは全て `async def` でイベントループ直列実行にして SQLite の書き込み競合を回避している(ローカル単一ユーザー前提)。ハンドラ内でブロッキングの長い処理(LLM 呼び出し等)を書くときはこの前提を崩さないよう注意(LLM 呼び出しは httpx の async 版を使う)。
- Phase 1 のノード削除は末尾のみ許可(単線維持のため)。

### 検索層の知見(2026-07-25)

- **trigram FTS は 3 文字未満の語を照合できない。** 日本語クエリは「語」抽出では機能しない(助詞込みで一続きになる)ため、クエリから文字トライグラムをストライド 1 で生成して OR 検索する(`retrieval._fts_terms`)。重み付けは bm25 に任せる。
- 埋め込みは書き込み時にモデルがロード済みの場合のみ行い、未処理分は検索時の `ensure_vectors` が自己修復する(初回ロード/ダウンロードで書き込みを塞がないため)。モデルは FastAPI startup でバックグラウンド warmup。
- Ruri のモデルキャッシュは `models/embeddings/`(gitignore 済み、約 1.2GB)。

### E2E 検証で得た LLM まわりの知見(2026-07-24)

- **llama.cpp のグラマー変換は JSON schema の `minimum`/`maximum` を無視する。** 数値の範囲制約はプロンプト指示 + 生成後の clamp(`generation.normalize_events`)で担保する。
- **条件付き required は表現できない。** fact_set の「scope=char なら char 必須」はスキーマを scope 別の 2 変種に分割して表現した。schema で強制しないと 12B は char を落とすことがあり、不正イベントが DB に入ると fold が壊れる(検証 `_missing_payload_fields` でも防御)。
- **グラマー制約下で空白のみを max_tokens まで生成し続けることがある**(Gemma + llama.cpp の既知事象、特に低温度)。finish_reason=length + 空内容として検出し、温度を変えてリトライで回避。
- SSE 生成器内で例外が漏れると接続が切れて原因が見えなくなる。`generate_beat_stream` は必ず error イベントに変換する構造にしている。
